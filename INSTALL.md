# 🧊 ПОЛЮС-11 RP — УСТАНОВКА ГОТОВОГО СЕРВЕРА

Сборка-гейммод: **ничего воркшопного не надо** — весь контент стоковый HL2/GMod.
Один гейммод содержит всё: профессии, F4, NPC-кадровика, админку с наказаниями,
Нечто с классами, заражение, генератор/блэкаут, анализ крови, рацию, задачи.

---

## ШАГ 1. Куда кидать файлы

В архиве лежит папка `garrysmod/`. Её содержимое копируется **поверх**
`garrysmod/` твоего сервера (совпадение папок = правильно):

| Что в архиве | Куда попадёт на сервере | Зачем |
|---|---|---|
| `garrysmod/gamemodes/polus/` | `garrysmod/gamemodes/polus/` | сам гейммод (вся логика сервера) |
| `garrysmod/cfg/server.cfg` | `garrysmod/cfg/server.cfg` | конфиг: хостнейм, лимиты, гейммод |

**НЕ ставь рядом старые аддоны** `polus_framework`, `polus11`, `prof_npc`, `thing_swep` —
гейммод уже содержит их код, двойная загрузка сломает хуки.

## ШАГ 2. Запуск сервера

Командная строка (пример):

```
srcds.exe -game garrysmod -console +gamemode polus +map gm_flatgrass -maxplayers 24 +host_workshop_collection 0
```

На хостинге в панели: **Gamemode = `polus`**, карта — любая.

## ШАГ 3. Сделать себя суперадмином

Файл `garrysmod/settings/users.txt` (если нет — создай):

```
"Users"
{
	"superadmin"
	{
		"ТвойНик"	"STEAM_0:0:12345678"
	}
}
```

(свой SteamID смотри на steamid.io). Или с консоли сервера, когда ты в игре:
`ulx` не нужен — фреймворк штатно видит superadmin/admin.

## ШАГ 4. Первичная настройка карты (5 минут, в игре)

Всё делается из админ-меню: **`p11fw_admin` в консоли** или кнопка **АДМИНКА** в F4, вкладка УТИЛИТЫ.

| Порядок | Действие | Как |
|---|---|---|
| 1 | Точка спавна гарнизона | встать на место → УТИЛИТЫ → «Точка спавна» (или `polus_fw_setspawn`) |
| 2 | Точка ареста (карцер) | встать в комнате-клетке → «Точка ареста» (или `polus_fw_setjail`) |
| 3 | NPC-кадровик | встать у стойки → «NPC-кадровик» (или `polus_fw_npc_create`) — сохраняется навсегда |
| 4 | Генератор + бочка с топливом | `!пульт` → спавн генератора/бочки |
| 5 | Лабораторный стол | `!пульт` → спавн лабстола |

Всё сохраняется в `garrysmod/data/` (по картам) и переживает рестарт/чистку.

## ШАГ 5. Проверка (смоук-тест)

В консоли сервера: `polus_status` — покажет версии, модули, энтити, заражённых.
Полный чек-лист из 17 пунктов — в `docs/ALPHA_READY.md`, раздел 2.

## Карты

Работает **любая** карта (persist привязан к её имени). Для атмосферы —
зимние/полярные карты (скачать вручную из воркшопа и закинуть в `garrysmod/maps/`),
для моментального теста — `gm_flatgrass`.

## Если что-то не грузится

1. Консоль сервера при старте: ищи строки `[POLUS][ERROR]` — там файл и строка.
2. `polus_status` — жив ли гейммод.
3. Вопросы/логи — в репозиторий: https://github.com/fieryartem2013-crypto/polus-11
