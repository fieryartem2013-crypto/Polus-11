# 📋 ПАТЧ-НОУТ v5.6.6 — «!-КОМАНДЫ БОЛЬШЕ НЕ УХОДЯТ В OOC»
**Дата:** 13.08.2026 · **Сборка:** POLUS11_SERVER_v5.6.6.zip

> 📌 Правило соблюдено: старые файлы не тронуты, всё новое.

## 🔧 ПРОБЛЕМА
Команды !дежурство / !опыт / !снятьдежурство уходили в **OOC** как
обычный возглас, потому что их НЕТ в белом списке BANG_SERVER роутера
чата (p11_sv_chat.lua — старый файл).

## ✅ РЕШЕНИЕ (надёжное)
`lua/autorun/server/p11_sv_bangfix_v566.lua` — оборачивает САМ хук
роутера "P11.ChatCore" (из hook.GetTable): перед его вызовом проверяет
наши команды, выполняет и возвращает "" (съедено, в OOC не уйдёт).
Не зависит от порядка/priority хуков — работает гарантированно.

## ✔️ РАБОТАЮТ
- `!дежурство` → открыть меню постов
- `!снятьдежурство` / `!снятьпост` → снять пост
- `!опыт` / `!выдатьопыт` / `!выдатьxp` → открыть админ-выдачу опыта

## ⚠️ УСТАНОВКА
1. Удали на сервере папки `garrysmod/lua/autorun/` и
   `garrysmod/gamemodes/polus/`.
2. Скопируй новую `garrysmod/` из архива ЦЕЛИКОМ.
3. Перезапусти сервер. Клиентам — перезапустить игру + удалить cache/.

## ПРОВЕРКА
- В чат: `!дежурство` → откроется меню (НЕ уйдёт в OOC).
- Консоль сервера: `[POLUS-11] ПЕРЕХВАТ !-КОМАНД v5.6.6: роутер обёрнут...`
