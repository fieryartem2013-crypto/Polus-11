# 📋 ПАТЧ-НОУТ v5.6.3 — «ФИКСЫ ПРИМЕНЯЮТСЯ ПОСЛЕ ГЕЙММОДА»
**Дата:** 12.08.2026 · **Сборка:** POLUS11_SERVER_v5.6.3.zip

> 📌 Правило соблюдено: старые файлы не тронуты, всё новое.

## 🔧 ПОЧЕМУ ФИКСЫ v5.6.2 НЕ РАБОТАЛИ
Они лежали в autorun/client, который грузится ДО гейммода. Когда фикс
искал хук `P11.ThingHUD` и функцию `P11.OpenReports` — их ещё НЕ было
(гейммод не загрузился) → фиксы молча не срабатывали.

## ✅ v5.6.3 — ВСЁ ПРИМЕНЯЕТСЯ ПОСЛЕ ЗАГРУЗКИ ГЕЙММОДА
`lua/autorun/client/p11_cl_fixes_v563.lua`:
- применение через PostGamemodeLoaded + InitPostEntity + таймеры
  (0/1/2/5 сек) — хуки/функции уже существуют → фиксы срабатывают.
- всё в pcall — не падает.

## 🎯 1) HUD НЕЧТО ПОВЕРХ ОСНОВНОГО
Исходный хук "P11.ThingHUD" вынесен из HUDPaint в DrawOverlay —
рендерится ГАРАНТИРОВАННО ПОСЛЕ всего HUD.

## 📣 2) РЕПОРТЫ — HUD-БАННЕР ВМЕСТО ОКНА
При новом репорте у админа: не-блокирующий баннер (справа сверху)
«НОВЫЙ РЕПОРТ» + текст + «!репорты — открыть окно (стреляй дальше)».
Ввод не перехватывает, гаснет через 12 сек. Окно — только по !репорты.

## 🔥 3) HUD ТОПЛИВА ОГНЕМЁТА — КРАСИВЕЕ
Перерисован: градиентный бар (оранжевый→красный), свечение,
пульс-мигание при малом топливе, заголовок «КУСТАРНЫЙ ОГНЕМЁТ»,
счётчик «топливо / макс», предупреждение «НЕТ ТОПЛИВА».

## ⚠️ УСТАНОВКА
1. Удали на сервере папки `garrysmod/lua/autorun/` и
   `garrysmod/gamemodes/polus/`.
2. Скопируй новую `garrysmod/` из архива ЦЕЛИКОМ.
3. Перезапусти сервер.
4. Клиентам: перезапустить игру + удалить `garrysmod/cache/`.

## ПРОВЕРКА
- Консоль клиента: `[POLUS-11] МАСТЕР-ФИКСЫ v5.6.3 ...`
- HUD Нечто поверх панели HP; репорт → баннер; огнемёт — красивый бар.
