# 📋 ПАТЧ-НОУТ v5.7.0 — «ОТКАТ ПЕРЕКРАСА ТЕКСТА + МЕДАЛИ ВЫРЕЗАНЫ НАСМЕРТЬ»
**Дата:** 14.08.2026 · **Сборка:** POLUS11_SERVER_v5.7.0.zip

> 📌 Правило соблюдено: старые файлы не тронуты, всё новое.

## 🎨 1) ТЕКСТ: ГЛОБАЛЬНЫЙ ПЕРЕКРАС ОТКАЧЕН
- Проблема: агрессивный фикс «белый текст» (v5.6.5) перекрашивал ВСЕ
  DLabel/DButton глобально → там, где нужен чёрный — стал белым и слился
  с меню, и наоборот.
- Решение: глобальные патчи (vgui.Create, skin.colours, DButton.Paint)
  **УБРАНЫ** — возвращён штатный рендер текста. Ничего глобально не
  перекрашиваем. Точечные правки — отдельно, при необходимости.

## 🎖 2) МЕДАЛИ — ВЫРЕЗАНЫ НАСМЕРТЬ
Клиент (fixboot, v5.7.0): `P11.MedalCells/MedalTop/MedalScopeLocal/
MedalAwardMenu` = пустые/нил →
- TAB не рисует ленту медалей, доску почёта, карточку медалей;
- нет кнопки «Вручить медаль»;
- вкладка «МЕДАЛИ» в админке показывает «отключено» (уже было).
Сервер (v525_autorun): `MedalPush/Award/Scope/AutoGrant` = no-op,
`Medals = {}` — выдача/синк невозможны.
Итог: медалей нет нигде — ни в TAB, ни во вкладке, ни над головами.

## ⚠️ УСТАНОВКА
1. Удали на сервере папки `garrysmod/lua/autorun/` и `garrysmod/gamemodes/polus/`.
2. Скопируй новую `garrysmod/` из архива ЦЕЛИКОМ.
3. Перезапусти сервер. Клиентам — перезапустить игру + удалить cache/.

## ПРОВЕРКА
- Текст в меню — как раньше (штатные цвета, ничего не сливается).
- TAB — без медалей (лента/доска/кнопка «Вручить» исчезли).
- Консоль клиента: `[POLUS-11] v5.7.0: медали вырезаны насмерть...`
