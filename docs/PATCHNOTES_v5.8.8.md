# 📋 ПАТЧ-НОУТ v5.8.8 — «О НАС: КНОПКА ВПИСАНА В С-МЕНЮ»
**Дата:** 16.08.2026 · **Сборка:** POLUS11_SERVER_v5.8.8.zip

> 📌 Правка по просьбе владельца: кнопка «О НАС» вписана ПРЯМО
> в С-меню (v5.8.7 ставила её обёрткой поверх — перекрывала
> другие кнопки, визуально некрасиво).

## 🔘 ЧТО ИЗМЕНИЛОСЬ
`gamemodes/darkrp/gamemode/modules/p11_cl_cmenu.lua` (правка v5.8.7):
- внизу С-меню добавлен **баннер «ℹ О НАС»** (стиль ДРЕВО/ОПЕРАЦИИ,
  на всю ширину) — «общество „Проект Арчи“ · наш Discord · коллекция моделей»;
- окно чуть выше: обычные 600 → **652**, стафф 678 → **748**;
- подсказка внизу сдвинута, чтобы освободить место баннеру;
- баннер НИЧЕГО не перекрывает: у обычных — под ДРЕВО/ОПЕРАЦИИ,
  у стаффа — под полосой быстрых команд.

`gamemodes/darkrp/entities/entities/polus_p11_about/cl_init.lua`:
- убрана обёртка-кнопка (AddAboutButton + патч OpenCMenu) —
  больше НЕ вешается кнопка поверх (было перекрытие);
- остались: окно «О НАС», команда `!о нас`, автопоказ новичку.

## 📋 ЧТО ОСТАЛОСЬ (из v5.8.7)
- Окно «ОБЩЕСТВО „ПРОЕКТ АРЧИ“»: текст про общество, кнопки
  «💬 НАШ DISCORD» и «📦 НАША КОЛЛЕКЦИЯ» (открывают браузер);
- автопоказ новичку при первом входе (один раз);
- `!о нас` / `!проект` / `!об обществе` — открыть окно.

## ⚠️ УСТАНОВКА
1. Удали на сервере папки `garrysmod/gamemodes/darkrp/` и `garrysmod/lua/autorun/`.
2. Скопируй новую `garrysmod/` ЦЕЛИКОМ.
3. Перезапусти сервер. Клиентам — перезапустить игру + удалить cache/.

## ПРОВЕРКА
- Открой С-меню (C): внизу баннер «ℹ О НАС» — ничего не перекрывает,
  все кнопки на местах. Клик → окно «О НАС» с Discord и коллекцией.
