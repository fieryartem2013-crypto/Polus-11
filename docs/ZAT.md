# 🔒 ЗАТ — ЗАКРЫТЫЙ АЛЬФА-ТЕСТ «ПОЛЮС-11» (v3.8)
Дата: 05.08.2026. Сборка **4.0.0** (P11FW 1.9.0 + POLUS11 3.2).

## 0. ПЕРЕД ЗАПУСКОМ — 3 обязательных шага

1. **Секретный ключ Главы** — уже выставлен владельцем:
   `FounderKey = "АрчиславКрутойПарень2013"`
   (файл `gamemodes/polus/gamemode/modules/fw_sh_config.lua`).
   Вход по ключу — в консоли **клиента** (своего игрока): `p11_access АрчиславКрутойПарень2013`
   → ранг «Глава Полюса-11» + superadmin. При желании ключ можно сменить там же — тогда
   раздай новый только своим. Попытки ввести неверный ключ пишутся в лог сервера.
2. **Пароль**: с v3.8.1 пароль УБРАН по просьбе владельца (`sv_password ""` — вход свободный).
   Хочешь закрыть — впиши `sv_password "свой"` в `garrysmod/cfg/server.cfg`.
3. Поднять карту, в консоли сервера: `polus_status` — модули **23/23**,
   энтити по нулям, версии 1.8.0/3.2/3.8.1 в логе стартапа.

## 1. РАНГИ И ПРАВА МОДЕРАЦИИ (v1.6)

Чем выше ранг — тем больше команд и тем ДЛИННЕЕ срок бана/мута/ареста.
Защита иерархии: наказать можно только того, чей ранг **ниже твоего**
(Глава — всех, кроме других Глав).

| Ур. | Ранг | Варн | Мут | Кик | Арест/рабство | Быстрые действия* | Бан | Разбан | Ранги |
|---|---|---|---|---|---|---|---|---|---|
| 0 | User | — | — | — | — | — | — | — | — |
| 1 | VIP | — | — | — | — | — | — | — | — |
| 2 | Хелпер | ✅ | до 30 мин | — | — | — | — | — | — |
| 3 | Модератор | ✅ | до 4 ч | ✅ | до 1 ч | — | — | — | — |
| 4 | Админ | ✅ | до 12 ч | ✅ | до 3 ч | ✅ | до 3 сут | — | — |
| 5 | Куратор | ✅ | до 1 сут | ✅ | до 1 сут | ✅ | до 7 сут | — | ✅ ниже своих |
| 6 | Суперадмин | ✅ | до 7 сут | ✅ | до 3 сут | ✅ | до 30 сут | ✅ | ✅ |
| 7 | Основатель | ✅ | до 14 сут | ✅ | до 7 сут | ✅ | до 70 сут | ✅ | ✅ |
| 8 | Создатель | ✅ | до 30 сут | ✅ | до 7 сут | ✅ | до 300 сут | ✅ | ✅ |
| 9 | Глава Полюса-11 | ✅ | безлимит | ✅ | безлимит | ✅ | **НАВСЕГДА** | ✅ | ✅ |

\* Быстрые действия = лечить/возродить/тп/заморозить/убить (вкладка ДЕЙСТВИЯ).

- Пункт админки **МОДЕРАЦИЯ** (2-я вкладка): выбери игрока слева — справа варн,
  мут/снять мут, кик, очистка варнов и БАН с выбором срока (дни/часы/минуты или
  галочка «НАВСЕГДА» — доступна только Главе). Кнопки без прав — серые.
- **Варны**: 3-й варн = автоматический КИК (порог: `AutoKickWarns` в fw_sh_config.lua).
- **Мут**: перекрывает чат, значок над HUD жизни, снятие по таймеру.
- **Журнал**: каждое действие админов пишется в `data/polus_framework/modlog.txt`
  и в консоль сервера (`[P11FW][MOD]`).
- Консольный паритет: `p11_mod_warn/mute/unmute/kick/ban/unban/unwarn/warns`.
- Данные: `data/polus_framework/{warns,mutes,bans,ranks,punish}.json`.

## 2. HUD ЖИЗНИ (v2.8)

Слева внизу — ЗДОРОВЬЕ и БРОНЯ (плавные бары, пульс и красная виньетка при
критическом HP), справа внизу — ПАТРОНЫ активного оружия. Стоковый HL2-HUD
по-прежнему скрыт — теперь его полноценно заменяет наш.

## 3. ИНТРО v2 (v3.4)

Кино-полосы, снег, зерно плёнки, сканлайны, двухслойный ветер, ударная строка
красным и финальный светящийся логотип «ПОЛЮС-11» с радаром. Пропуск — клик/клавиша.
Ручной повтор: `p11_intro_show`. Отключить себе: `p11_intro 0`.

## 4. СВЕРХ-СМОУК-ТЕСТ v3.4 (дополнения к прошлым чеклистам)

| # | Проверка | ОК |
|---|---|---|
| 1 | В консоли клиента: `p11_access АрчиславКрутойПарень2013` → ты Глава + superadmin | ☐ |
| 2 | Хелпер: админка открывается, во вкладке МОДЕРАЦИЯ кликабельны только варн/мут/снять мут | ☐ |
| 3 | Модератор: кик работает, БАН — серая кнопка | ☐ |
| 4 | Админ банит на 5 суток → срок урезан до 3 суток с уведомлением; в modlog.txt строка BAN | ☐ |
| 5 | Галочка НАВСЕГДА активна только у Главы; перма ставится, в бан-листе «НАВСЕГДА» | ☐ |
| 6 | 3 варна одному игроку → авто-кик через ~1 сек; варны обнулились | ☐ |
| 7 | Мут: игрок пишет в чат → чат пуст, ему сообщение «ты в муте»; значок «МУТ N мин» на его HUD | ☐ |
| 8 | Админ не может варнить/банить Куратора (ранг выше) — отказ с текстом | ☐ |
| 9 | Разбан: у Админа кнопка «СНЯТЬ БАН» серая, у Суперадмина+ активна | ☐ |
| 10 | HUD жизни: урон → бар жёлтеет/краснеет пульсом, виньетка по краям; броня синим; патроны меняются при выстреле | ☐ |
| 11 | Интро: полосы въезжают, снег идёт, красная строка, логотип с радаром; пропуск кликом | ☐ |
| 12 | В TAB у Хелпера+ виден жёлтенький значок «ранг» рядом с ником | ☐ |

## 5. СМОУК-ТЕСТ v3.5 (новые идеи)

| # | Проверка | ОК |
|---|---|---|
| 1 | Замаскированное Нечто: через ~30 сек под маской появились 2 обычные задачи, прогресс ползёт сам; при явлении — пропали | ☐ |
| 2 | Терминал начальства: прогресс доп-задачи замаскированного Нечто показывает рост быстрее реального (ложь для офицера) | ☐ |
| 3 | Офицер: `!приказ Всем построиться` → у всех золотой баннер 12 сек + задача «построение» засчитана офицеру | ☐ |
| 4 | Не-офицер пишет `!приказ` → отказ; админ может | ☐ |
| 5 | `!розыск ник` → у цели красная плашка «ВЫ В РОЗЫСКЕ», над головой «⚠ РОЗЫСК» мигает, в TAB метка; повторная команда — снять | ☐ |
| 6 | Распорядок: раз в ~3.5 мин метка вверху справа меняется + гудок + строка в TAB (фаза сюжета у пульта НЕ ломается) | ☐ |
| 7 | Авто-метель: через 20-30 мин после старта — метеосводка, потом буря: косой снег, изморозь, ветер, рация глушится | ☐ |
| 8 | Админ: `!буря` вкл/выкл буру вручную | ☐ |
| 9 | Терминал: кнопка «◍ СЕБЕ» — задача взялась самому дежурному | ☐ |
| 10 | `!репорт тест` → админам в чат, строка в `data/polus11/reports.txt`; `!ролл` — кубик всем в чат | ☐ |
| 11 | У Хелпера+ над головой виден ранг цветом (под должностью) | ☐ |

## 6. СМОУК-ТЕСТ v3.6 (С-меню, документы, F4)

| # | Проверка | ОК |
|---|---|---|
| 1 | Удерживаешь C → станционное меню (не песочница); жми «👋 Махнуть» → окружающие видят взмах | ☐ |
| 2 | Админ: в C-меню «🧍 Меню моделей» → браузер, поиск, клик надевает модель; `p11_wt_modelreset` возвращает | ☐ |
| 3 | `/menu` в чате у админа открывает админ-панель (старые !fw тоже живут) | ☐ |
| 4 | У всех в слотах: «Пустые руки» + «Документы» (+ кулаки/физган как было) | ☐ |
| 5 | «Документы»: свой код виден на HUD; ЛКМ/R у игрока рядом → у него карточное окно с КОДом и печатью | ☐ |
| 6 | Замаскированное Нечто предъявляет документы → в окне УКРАДЕННОЕ имя жертвы | ☐ |
| 7 | F4: крупное окно 1020×640, у проф — значок ⌨ терминала / ★ командира, живая карта должности, кнопка «АДМИН-ПАНЕЛЬ (/menu)» | ☐ |
| 8 | Админка → ФРАКЦИИ: справа вверху формы живой цветной превью-чип меняется с ползунками | ☐ |
| 9 | По углам экрана вечная лёгкая изморозь; в бурю заметно сильнее | ☐ |
| 10 | Арестованный жестами махать не может (кнопки есть — анимации нет) | ☐ |

## 7. СМОУК-ТЕСТ v3.7 (мороз, машинное, новые профы)

1. **Модели**: спавни через пульт(!) сменный терминал / лаб-стол / генератор — если стоит пак lt_c, будут новые модели; без пака — старые (оба варианта ОК, ошибок быть не должно).
2. **Износ генератора**: вставь генератор, подожди (или `p11_storm`), смотри шкалу ИЗНОС на генераторе; после 50% жди поломку (перегрев/утечка/стартер/скачки) — идёт ~каждые 12 сек ролл.
3. **Техосмотр**: держи E по генератору при поломке — прогресс-бар, в конце «Поломка устранена», износ 0. За это засчитывается задача техника gen_service.
4. **РЕЖИМ РЕЗЕРВ**: присядь (CTRL) + E по генератору (нужен техсостав/офицер/админ) — режим переключается, на 3D-табличке «РЕЗЕРВ · ГОТОВ К АВАРИИ». Отключи основной (стрельни) — резервный продолжит держать свет.
5. **Грузчик**: возьми профу Грузчик в F4 → E по бочке — на плечо (скорость чуть меньше), донеси до генератора → E — генератор заправлен, бочка пропала, задача haul +1 (и refuel_gen).
6. **Медик**: профа Полевой медик → подойди к раненому, ПКМ шприца — +12 ХП, у цели звук аптечки, тебе задача heal_player. Кулдаун на цель 20 сек.
7. **Холод**: стой на улице (под открытым небом) 1-2 мин — бар ❄ ТЕПЛО внизу слева падает, на улице в бури быстрее. < 30 — дрожит прицел, лёд по краям, пульс «ТЫ ЗАМЕРЗАЕШЬ». < 12 — по 2 ХП/2с урон. Встань у генератора — тепло летит вверх ×9. Паёк повара даёт +15 тепла. Нечто в активной форме не мёрзнет (иммунна).
8. **TAB**: обычный игрок видит онлайн «~N (±)» и число плавает раз в ~10 сек; админ (Хелпер+ или серверный админ) — точное N. Сортировка: внутри фракции админы сверху по рангу. У игрока в муте — значок 🔇. Ранги Куратор+ переливаются (shimmer), Создатель/Глава — aurora.
9. **Конфликт меню**: быстро открывай-закрывай F4/админку/терминал/TAB — в консоли НЕ должно быть спама `dscrollpanel.lua:111 NULL Panel` (допустима одна строка «layout guarded» при самом первом пропуске кадра).
10. **Ваниль**: при TAB НЕЛЬЗЯ увидеть стандартный серый TAB песочницы поверх нашего стеклянного.


## 8. СМОУК-ТЕСТ v3.8 (вид/курсор/код, починка C-меню)

1. **C-меню**: просто зажми C — станционное меню ОТКРЫВАЕТСЯ сразу (выезд снизу +
   затемнение экрана). Отпустил — закрылось. В консоли НЕТ ошибок. У админа (Хелпер+)
   справа секция «АДМИНИСТРАЦИИ» → «Меню моделей» открывает браузер моделей.
2. **Еррорки моделей**: в меню моделей отсутствующие на ПК плеер-модели показаны
   красными ячейками «ВОРКШОП? нет на ПК» (не валятся в розовый ERROR). Чтобы модель
   появилась у всех — впиши ID пака в `P11FW.Config.WorkshopAddons` (fw_sh_config.lua)
   и рестарни сервер.
3. **F2**: нажми F2 — камера за спиной (модель видна, углы не обрезает). F2 ещё раз —
   первое лицо. В углу комнаты камера не уходит за стену.
4. **F3**: нажми F3 — курсор отцепился (наводка-подсказка внизу «курсор свободен»).
   Клик мышью по миру стрелять не мешает? (докладывай, если неудобно). F3 — прицел назад.
5. **Документы**: предъяви документы кому-нибудь (ЛКМ/R), запиши код. Умри/ресуйся —
   код в HUD-подсказке SWEP'а ТОТ ЖЕ. Выйди-зайди — код новый. Админка → ИГРОКИ —
   колонка КОД совпадает с HUD.
6. **Голос**: попроси второго тестера поговорить — зелёные «панельки-громкоговорители»
   слева внизу НАД панелью состояния (не поверх правого HUD и не поверх имени над головой;
   над головой говорящего — маленький 🔊 под табличкой). У Нечто под маской в панельке —
   украденное имя.
7. **Палитра цвета**: /menu → ДОЛЖНОСТИ → клик по цветному квадрату — окно DColorMixer
   (колесо/числа). Клик по квадратикам-пресетам меняет цвет. То же во вкладке ФРАКЦИИ.
   Создай тестовую профу — цвет сохраняется в F4.
8. **Фракции видно**: /menu → ДЕЙСТВИЯ — в списке игроков колонка ФРАКЦИЯ (цветная).
   Над головами — строка `фракция · должность`.
9. **Края экрана**: в ясную погоду без холода по краям НИЧЕГО нет (вечную изморозь
   убрали). В бурю/на морозе лёд появляется, как раньше.
10. **Баннер приказа**: офицер !приказ — список задач слева на время сползает вниз,
    не наезжая на баннер.


## 8.1 СМОУК-ТЕСТ v3.8.1 (хотфикс)

1. **TAB**: жми TAB 5-10 раз подряд, пока кто-то выходит/заходит — краша/спама не быть;
   если красивое табло сломалось дважды, поднимется «аварийный режим» (простой список —
   состав всегда виден). В консоли ищи строку `[POLUS][ERROR] TAB open:`.
2. **Редактор встроенных**: /menu → ДОЛЖНОСТИ → выбери ВСТРОЕННУЮ (напр. «Лаборант»),
   поменяй оружие (+ОРУЖИЕ В РУКАХ) и модель, жми «СОХРАНИТЬ ПРАВКУ» — статус
   «сохранено как ПРАВКА». Зайди в F4 и ВОЗЬМИ эту пр (уже пропатченную) — модель
   сменилась, оружие ВЫДАНО. В списке тип «ПРАВКА». Кнопка «УДАЛИТЬ/СНЯТЬ» — правка
   снята, вернулась заводская. То же во ФРАКЦИЯХ (цвет/название встроенной меняются).
3. **Новые кастомные**: создай новую пр из /menu — появится в списке и в F4 мгновенно,
   переживает рестарт (jobs_custom.json).
4. **Свои документы**: ПКМ или R с SWEP «Документы» — открывается твоя карта.
   ЛКМ рядом с человеком — предъявление, как раньше.
5. **Темп**: бег заметно медленнее, прыжок ниже; бхоп-цепочка разбег не разгоняет
   (солдатский шаг!). Грузчик с бочкой — пропорционально медленнее, после E — назад.
6. **Пароль**: сервер пускает БЕЗ пароля.

## 8.2 СМОУК-ТЕСТ v3.8.2 (хотфикс «ТЕПЛЫЙ СТАРТ» + сид РККА)

**Крит-баги, которые лечит:**
- вечная ошибка `p11_cl_vitals.lua:111: attempt to perform arithmetic on global 'curWarm'` (xБЕСКОНЕЧНО в консоли) — убита в корне;
- «получаю урон просто так и умираю» — это был мороз без объяснений: теперь 150 сек передышки после спавна + громкий крик «ОБМОРОЖЕНИЕ!» при реальном уроне;
- правка встроенной фракции слетала после рестарта — починено.

**Прогон (12 минут):**
1. Зайди на сервер — в консоли (клиент) НЕ должно быть красных строк про curWarm; панель жизни слева внизу плавно дышит.
2. Только что заспавнился? Постой на улице 2 минуты — тепло НЕ уходит (передышка). Появится строка тепла в HUD.
3. После передышки выйди в метель: тепло падает медленно; при тепле < 12 приходит чат «ОБМОРОЖЕНИЕ! ...» на каждый тик урона. Вернись к генератору — восстановится.
4. F4 → появились фракции **РККА** и **НЕЧТО**, в гарнизоне/науке — новые должности из списка (Постовой РККА, Штурмовик, Генерал, Био-химик, Создатель Научного Комплекса...).
5. Возьми «Штурмовик РККА»: на руках ППШ (если стоит ARC9-пак), HP 125 / броня 145 (панель покажет 125 макс), в руках есть «Пустые руки».
6. Обычным игроком (или попроси тестера без админки) жми на «[ИВЕНТ] Нечто» — должен отказать: «ивентовая роль...».
7. Админом (p11_access ключ → Глава Проекта) возьми «[ИВЕНТ] Нечто-Громила» — свep Нечто на руках, HP 900/200.
8. F4 → Админ → Профы: кликни «Солдат РККА» — редактируй (это кастом-пресет), «Рядовой охраны» — тоже редактируй (встроенная, сохраняется правкой).
9. TAB — ранги новые: список из 16 (Helper → Глава Проекта) в админке → вкладка «Ранги/Игроки».
10. Рестарт карты/сервера: сид НЕ плодит дубликаты (в консоли сервера «Сид: ...» сработает только один раз), правки должностей/фракций на месте.
11. Воркшоп-паки моделей РККА (hts/comradebear) и учёных (UIF) впиши id-шниками в `P11FW.Config.WorkshopAddons` (fw_sh_config.lua) — иначе игроки видят ERROR-гигантов, это ожидаемо.
12. DarkRP-пак: файл `darkrp/jobs_p11.lua` из архива — если поднимаешь DarkRP-ветку сервера.

## 8.3 СМОУК-ТЕСТ v3.8.3 («ОСОБЫЙ ОТДЕЛ»)

1. Рестарт сервера (достаточно changelevel) → в консоли: `Сид: добавлено фракций-пресетов: 1` + `Сид: завезено должностей-пресетов: 5` (при ПЕРВОМ запуске на 3.8.3).
2. F4 → фракция **НКВД** с 5 должностями; старые профы РККА/Наука НЕ продублировались.
3. Возьми «Особист НКВД»: проверь `!приказ тест` и `!розыск <ник>` — должны работать (флаг command).
4. «Начальник Особого Отдела» — 125 ХП/125 брони, терминал доступа, 1 место всего.
5. F4 → Админ → Профы → отредактируй «Следователь НКВД» (смени цвет/модель) — сохраняется, переживает рестарт.
6. Хочешь «синие фуражки» для НКВД — впиши пути своего пака в админке (или пришли ссылку на воркшоп — добавлю в пресет и WorkshopAddons).

## 8.4 СМОУК-ТЕСТ v3.9 («КАДРЫ СТАНЦИИ»)

1. **Модельки**: возьми любую прокачку РККА (Постовой/Штурмовик...) — внешность СРАЗУ станет солдатом (не male_01). Если у тебя стоит воркшоп-пак — видишь форму, у игрока без пака — ERROR (это решается WorkshopAddons).
2. **Штаты**: F4 → встроенных «ВОЕННЫЙ ГАРНИЗОН» и старых проф (Охрана/Офицер/Лаборант/Вирусолог) больше нет. Есть: БЕЗ НАЗНАЧЕНИЯ (Новобранец), ПЕРСОНАЛ (Уборщик/Снабженец/Грузчик/Техник/Медик/Инженер), УЧЁНЫЕ + сид-фракции РККА/НКВД/НЕЧТО.
3. **Вакансия Нечто**: админ-команда `p11_offer` → в чате «ОСОБАЯ ВАКАНСИЯ...», у кадровика красная мерцающая строка. E по кадровику: меню НЕ открылось, в чат «Кадровик отводит взгляд...», через ~3 сек — ты активное Нечто (свep на руках). Второй жмущий E в то же окно — просто меню должностей. Подожди 90 сек: окно закрывается само, чат это подтверждает. Авто-окна идут раз в 4–8 мин (`ThingOfferGapMin/Max`).
4. **С-меню**: зажми C → «Меню моделей» теперь у всех. Раздел «МОЯ ДОЛЖНОСТЬ: ...» — клик по варианту СРАЗУ переодевает (не выходя из меню? — меню закроется, ты в новой модели). Админом видны все профы станции.
5. **Рация**: R (перезарядка) циклит «РККА → НКВД → Наука → Персонал → Общий»; `/r текст` слышат только свой канал.
6. **Задачи**: возьми Солдата РККА — виджет покажет «Нанеси урона Нечто / Доложи в рацию / Пост РККА». Учёного — «образцы крови/тест». Персонала — «топливо/хозработы».
7. **-compat**: уборщик с ломом, снабженец с пайком — на руках; грузчик носит бочки; техник 2× быстрее на генераторе; медик лечит ПКМ; построение !сбор засчитывает задачу «rollcall» Генералу/Особисту.
8. Полный рестарт: сид НЕ создаёт дубликатов НКВД; правки проф на месте.

## 8.5 СМОУК-ТЕСТ v4.0 («КАССА СТАНЦИИ»)

1. **Терминал**: админ → C-меню → «📍 Поставить» → «Сменный терминал» → ткни E: физика твёрдая (не парит), модель — лабораторный монитор; рестарт — терминал на месте (персист).
2. **Деньги**: выполни все задачи смены (или попроси тестера) → +2000₽ в чат-хинте и счётчике слева внизу. Доп-задача с терминала → +500₽. `/деньги` показывает баланс. Релог — кошелёк на месте.
3. **Ларёк**: «📍 Поставить» → «Ларёк снабжения» → E → витрина с ценами; купи «Горячий паёк» (250₽) → появился в 🎒 Багаж (C-меню) → «ИСПОЛЬЗОВАТЬ» → паёк в руках, копия списана.
4. **Сейф**: поставь → E → переложи ствол из инвентаря → перезайди на сервер → ствол в сейфе на месте.
5. **Оружие из EFT**: купи АКС-74У — если стоит ARC9-пак, ствол в инвентаре/руках; без пака — честный отказ «нет на складе», деньги не спишутся.
6. **Админ-табы**: зайди вторым клиентом/твинком с рангом Administrator (4): видит только МОДЕРАЦИЯ + ДЕЙСТВИЯ, остальные вкладки серые «с ранга Staff Leader». Рангом Staff Leader (14) — все вкладки открыты.
7. **Чат-команды**: `/tp <ник>` (перенёсся), `/return` (вернулся), `/bring <ник>` (притащил), `/cloak` (исчез, тень тоже), `/heal <ник>`, `/god`. Команда `/ранги` с рангом 10+ открывает админку на вкладке ИГРОКИ.
8. **Кадровик**: E по НПС — меню открывается (физика не глотает Use), вакансия Нечто из v3.9 продолжает работать.

### §8.6 Смоук v4.1 «СМЕННЫЕ ДЕЛА» (все галочки = релиз)
1. **Кадровик 2.0**: стойка НЕ T-pose (руки опущены, лёгкое покачивание); подойди близко — **голова плавно поворачивается за тобой**, отойди за 4 м — возвращается в нейтраль; тело НЕ вращается. E — меню должностей открывается с первого нажатия.
2. **Калибровка (учёный)**: возьми должность учёного, подойди к лабораторному столу БЕЗ колбы, жми E — в центре экрана появляется «КАЛИБРОВКА АНАЛИЗАТОРА» с буквами и сжимающимся кольцом. Нажимай [R/F/T/G] по букве. Успех ×4 → +60₽, «+1 RP», задача «Откалибруй анализатор» засчитана. Промах/отошёл — срыв, повтор через 60 сек.
3. **RP и грант**: сделай 5 RP в сумме (забор крови +1, тест +2, вскрытие +3, калибровка +1) → авто-выдача **250₽ «грант ЦНИИ»** + строчка в чат. Слева внизу счётчик 🔬 растёт.
4. **ТО генератора (техник)**: дождись износа 35+ (или поломки), жми E у генератора как ТЕХНИК — миниигра ТО вместо «держи E». Успех → износ −50, авария снята, +100₽, задача «техосмотр».
5. **Грязь (уборщик)**: подожди ~минуту живой игры на сервере — по станции появляются мешки грязи (до 6 шт). Уборщиком: E по мешку → 2 клавиши → +35₽, задача «Убери грязь». Другой профой — отказ «уборщик».
6. **Патруль (РККА)**: админом через УТИЛИТЫ → 📍 Расстановка поставь 3-4 «Поста патруля». Бойцом РККА: появился маркер 🚩 «ПОСТ №…» с дистанцией. E у поста + стоять 3 сек (не отпускать E) → +25₽, задача «Обойди посты патруля». Обойди ВСЕ → +150₽ и объявление в чат. После рестарта посты на месте (сейв в place-файле).
7. **Регрессии**: обычный (не техник) у генератора с поломкой — по-прежнему «держи E»; тест крови колбой работает как раньше; вкладки админки и /tp /cloak живы.
8. **UI**: F1 — новый стиль (акцентные подпункты, тэг версии в хедере); окна багажа/ларька/сейфа — тэг версии справа вверху.

### §8.7 Смоук v4.2 «БАРАХОЛКА И МУТАЦИИ» (все галочки = релиз)
1. **Повар**: админом поставь «🍲 Полевая кухня» (УТИЛИТЫ → 📍). Поваром — E, 3 клавиши → на плите горячий паёк; съешь другим персом: +15 ХП и тепло, повару +15₽ комиссия, задача «feed».
2. **Медик**: купи в ларьке «Ампула» (300₽). Медиком ПКМ шприца по раненому — у медика миниигра ИНЪЕКЦИЯ, пациент +25 ХП, ампула −1, +40₽. Без ампул — старая быстрая обработка +12.
3. **Грузчик**: вторым клиентом C→📦 «нужна бочка у лабы». У грузчика звук, маяк 📦 с текстом и дистанцией + пунктир линии пути по земле. Подойди заявителем к грузчику — оба получают подтверждения, грузчику ₽ (быстрее = больше).
4. **Сброс снабжения**: `p11_supply` (админ-консоль) — объявление, оранжевый маяк-столб. У ящика держи E 8 сек (индикатор %) — добыча падает в 🎒 Багаж (+ствол из списка), +тепло.
5. **Скидка дня**: ларёк — один товар в огненной рамке 🔥 −40%, цена снижена, покупка по сниженной.
6. **Досье НКВД**: особистом `/досье` — лента карточек (тест крови с результатом, арест, розыск, подмена помечена красным). Обычным бойцом — отказ «только особый отдел».
7. **Мутации**: админом возьми «[ИВЕНТ] Нечто» (теперь ОДНА профа). Убей/поглоти 3 жертвы → HUD «МУТАЦИЯ: РЕГЕНЕРАЦИЯ», ХП зарастает; 5 → МЯСОГИГАНТ (+60 ХП, когти больнее); 10 → АРАХНА (паучий облик при явлении, бег/прыжок). Справа внизу — счётчик жертв и шкала до тира.
8. **Итоги смены**: `p11_awards` — в чат титулы трёх лучших + ₽, у них **★ звание в TAB**.
9. **Античит**: спам-эмулятор не нужен — убедись, что обычная игра (миниигры подряд, закупки) НЕ флагается; телепорт админом `/tp` не ругается (льготное окно), самостоятельное «прыгнуть через карту» без прав — алерт админам.
10. **Туториал**: на чистом клиенте (или `p11_tutorial`) — 5 карточек с голубыми маяками: кадровик → дела → ларёк → генератор → F1.
11. **Регрессии**: рубли/задачи/патруль/вкладки — на месте; `luac` 130/130 чисто; TAB не падает и с игроком в коннекте.

### §8.8 Смоук v4.2.1 «ТАБ v2 И КАРТОЧКА ЖЕРТВЫ» (все галочки = релиз)

1. **TAB v2**: открой TAB — табло рисуется сразу, без панелей: фракции, ★звания,
   коды документов, пинг/розыск/мут. Колесо вниз при 20+ ботах (`bot` ×25) —
   список листается, края обрезаны чисто. Отпусти TAB — закрылось. В консоли
   клиента НОЛЬ строк `[POLUS][ERROR]` и NIL-цветов. Повтори с ботом в коннекте.
2. **Кадровик**: подойди к нему — в консоли клиента НЕТ потока ошибок
   (старый spam `GetPoseParameterIndex` убит); голова плавно следит за тобой
   в радиусе ~420, тело на месте; E открывает меню должностей.
3. **Кража личности (ядро)**: Нечто → убей игрока когтями → R по трупу.
   Уведомление: «Личность поглощена: «ИМЯ» · ДОЛЖНОСТЬ · док. КОД». В TAB
   все видят ИМЯ и ДОЛЖНОСТЬ жертвы в твоей строке (админ — + «(наст.: …)»).
4. **Документы жертвы**: возьми свеп «Документы» — в HUD твой код = КОД ЖЕРТВЫ.
   ЛКМ предъяви другому: в окне «ДОКУМЕНТ» имя/должность/фракция/код — ЖЕРТВЫ.
5. **Чат и неймтаг**: скажи что-нибудь в чат — твоё сообщение от ИМЕНИ ЖЕРТВЫ
   цветом её должности; сосед видит над твоей головой имя и должность жертвы.
6. **Возврат**: R без трупа через 5+ сек — облик/ник/должность/код снова твои
   (в TAB и в документах — своя карточка).
7. **Цепочка**: нечто-А съел игрока, нечто-Б съел труп А (после его смерти) —
   Б носит карточку той же жертвы.
8. **Регрессии**: мутации/досье/кухня/грузчик/снабжение/скидка/итоги/античит —
   как в §8.7; `luac` всех lua-файлов чист; в консоли сервера только
   «[POLUS-11] TAB v2 загружен» и штатные логи.

### §8.9 Смоук v4.2.2 «КОШЕЛЁК И СИЯНИЕ» (все галочки = релиз)

1. **Деньги НЕ за HUD'ом**: в игре слева виден «золотой кошелёк» (монета ₽,
   сумма с запятыми, подпись НАЛИЧНЫЕ) СТРОГО НАД панелью «СОСТОЯНИЕ БОЙЦА».
   Проверь на 1920×1080 и 800×600 — пересечений нет. Значок МУТА выше кошелька.
2. **Блеск прихода**: `polus11_givemoney` себе 500 — кошелёк вспыхивает золотом.
3. **Панели**: ледяная кромка сверху у панели жизни и у панели патронов;
   кончай магазин до ~20% — красный пульс контура боеприпасов.
4. **Интро v3**: перезайди (или сбрось `p11_intro 1` + реконнект) — полярное
   сияние «дышит» у горизонта, крупный снег впереди, штамп «ЭКСПЕДИЦИЯ-4»
   слева внизу мигает, справа внизу — кнопка **[ ПРОПУСТИТЬ ▶▶ ]**: клик —
   интро завершается со звуком, ты в игре. Любая клавиша/клик мимо — тоже.
5. **Пропуск обучения**: `p11_tutorial` — карточка сверху с «кнопкой» N.
   Зажми N на 2 сек — прогресс-полоса, обучение закрыто, файл записан,
   повторно само не стартует. `p11_tutorial_skip` — мгновенный тот же эффект.
6. **С-меню v4.2.3**: держи C — меню жестов выезжает снизу, мут экрана,
   колонки ЖЕСТЫ/БЫСТРОЕ, жест махает в мир. Если нативный бинд мёртв — то же
   самое делают сторож (просто зажми C), `p11_cmenu`, чат `!меню`. Открыл чат —
   C не открывает меню поверх. ESC/отпуск C — закрывается.
7. **Кадровик финал (§v4.2.3)**: стоит в стойке idle (никакой T-pose),
   голова следит; подойди на 1.5 м и жми E ДАЖЕ не в упор прицелом — меню
   проф открылось. Админом: `p11_npcmodel "models/player/gman_high.mdl"` —
   модель сменилась мгновенно и переживает рестарт; `p11_npcmodel reset`.
8. **Нечто гуманно (§v4.2.3)**: ПКМ укол по напарнику ВПЛОТНУЮ сбоку — цель
   ловится конусом; убей его когтями → R на труп с 2+ метров съедает; в чате
   тебе сводка украденной карточки (ник·должность·документ), у окружающих в
   TAB/над головой/в документах — карточка жертвы.
9. **Контент-чистка**: в Q-меню свепов «thing split/brute/spore» больше НЕТ
   (форма переключается `!форма`); серверный luac 130→129 (darkrp-черновик
   переехал в docs/legacy).
10. **Регрессии**: TAB v2, кадровик, личность/документы нечто

### §8.10 Смоук v4.3.0 «ДЕЛО БОЙЦА» (все галочки = релиз)

1. **Первый заход**: удали `data/polus11/chars.json` на сервере, зайди игроком —
   через ~6 сек окно «ЛИЧНОЕ ДЕЛО БОЙЦА». Заполни позывной (<3 знаков — отказ
   и повтор окна) и описание — «Дело записано». Перезайди — анкета НЕ
   открывается, данные на месте. `/персонаж` — правка.
2. **Взгляд**: второй клиент — смотри на бойца: над головой «ст. сержант
   КРАСНОВ», ВЫШЕ плавно проявляется карточка с описанием (до 3 строк + …).
   Отвёл взгляд — плавно гаснет. ХП нигде не светится.
3. **Позывной везде**: TAB (новая строка §v2) показывает позывной; предъяви
   документы — в окне позывной; чат-ник остался из имени удостоверения.
4. **Нечто и анкета**: Нечто съел бойца с анкетой — у смотрящих на него и
   позывной, и описание ЖЕРТВЫ; админ в TAB видит «(наст.: …)». R — свои.
5. **Деньги Главы**: получи 16-й ранг (`p11_access …`), в чат:
   `!дать Краснов 300` — цель +300₽, у обоих сводка; `!дать Краснов -50` —
   забрать. Рядовой игрок — отказ с пояснением ранга. Консоль `p11_givemoney`
   по-прежнему работает (5+).
6. **Регрессии**: §8.7–8.9 (TAB v2, кадровик, C-меню, кошелёк, личины);
   `luac` чист, консоль без красного., кухня/снабжение —
   как в §8.7–§8.8; `luac` чист; консоль клиента без красного.

### §8.11 Смоук v4.4.0 «ВАЙТЛИСТ» (все галочки = релиз)

1. **C-меню (реворк)**: зажми C — открылось; держи — висит; отпустил (~0.35 с)
   — закрылось. Повтор: `p11_cmenu` (тоггл), `!меню` в чат (тоггл), ESC.
   В клиентской консоли строка `[POLUS-11] С-меню v4.4.0 загружено`.
   Кнопки: жесты (анимации видны), «🪪 Мой персонаж», «🧍 Меню моделей»,
   «📦 Грузчик», «🎒 Багаж», у админа — «🛡 Админ-панель» и «📍 Поставить».
2. **Браузер внешности (с нуля)**: C → «🧍 Меню моделей» → раздел
   «МОЯ ДОЛЖНОСТЬ» — клик по иконке (превью справа), «НАДЕТЬ СЕБЕ». Чужая
   профа — кнопка «🔒» + отказ в чат. Админ: «ВСЕ ДОЛЖНОСТИ»/«СТАНДАРТНЫЕ»
   — надевает любую; «ВЫДАТЬ ИГРОКУ» (дроп-лист) — модель наделась ДРУГОМУ.
   «☆ В ИЗБРАННОЕ» — пункт появился в разделе «★ ИЗБРАННОЕ» и переживает
   перезаход клиента. Красные ячейки — модели без пака, превью-подстановка
   и статус «нет на твоём ПК».
3. **Вайтлист: завод**: `/menu → ДОЛЖНОСТИ → выбери любую профу → галочка
   «🔒 ВАЙТЛИСТ» → СОХРАНИТЬ ПРАВКУ` — в списке рядом с именем замок, в F4
   чип «🔒 ВАЙТЛИСТ», кнопка «🔒 НУЖЕН ДОПУСК» для игрока без допуска.
   Убедись: 5 должностей НКВД после обновления уже с замком (миграция сида).
4. **Выдача допуска**: вкладка ВАЙТЛИСТ → слева должность НКВД → выбери
   игрока → «✔ ВЫДАТЬ ДОПУСК» — у цели в F4 кнопка стала «ЗАНЯТЬ ДОЛЖНОСТЬ»,
   он берёт профу. «✖ СНЯТЬ ДОПУСК» на игроке НА должности — снят+уволен.
   Руками: `STEAM_0:x:y` в поле — выдаётся офлайн-игроку; `p11_wl_list`
   в консоли показывает всё.
5. **Ранги Faction Officer/Leader**: выдай через АДМИНКИ себе
   `faction_officer` (уровень 1 — админки НЕТ: вкладки МОДЕРАЦИЯ и пр. серые,
   `!фвадмин` пишет «только администрации» — через `p11fw_admin` открывается
   ОДНА вкладка ВАЙТЛИСТ; кнопка «🔒 Вайтлист» есть и в C-меню). Выдача/
   снятие допусков работает, `P11FW` допуски реально применяются.
6. **Вкладка АДМИНКИ**: открой `/menu → АДМИНКИ` — плитка из 19 рангов
   (User с приставкой «СНЯТЬ АДМИНКУ», Faction Officer/Leader с «🔒вайтлист»,
   Глава с аурой). Выбери игрока → клик «Helper» — ранг выдался (TAB,
   надголовье, строка в списке). Курьёз с пробелами в нике не ломает выдачу
   (идёт по SteamID).
7. **Кадровик навсегда**: поставь кадровика (`/menu → УТИЛИТЫ → СОЗДАТЬ`),
   перезапусти карту — на месте. Удали его кнопкой Z (англиш-undo) — лог
   «вычеркнут из сейва», перезапуск — НЕ воскрес.
8. **Регрессии**: §8.7–8.10 (TAB v2, админка ИГРОКИ/МОДЕРАЦИЯ/ДЕЙСТВИЯ,
   анкета, `!дать`, личины Нечто); `luac` чист; консоль без красного.

**Отмазки не принимаются, пока не проверены пп. 1–8.**

### §8.68 Смоук v4.19.2 «ШЛЮЗ» (все галочки = релиз)

Маркеры: `сборка 4.19.2` • сервер: `[POLUS-11] «ГЛАЗ» … v4.19.2 «ШЛЮЗ»` •
клиент: `[POLUS-11] «ГЛАЗ»: пульт диспетчера v4.19.2 «ШЛЮЗ» OK`.

- [ ] **E выводит**: сеанс за терминалом → `E` → тело свободно, пульт закрыл
  тебя с текстом «Сам встал из-за пульта».
- [ ] **ESC выводит**: снова сеанс → `ESC` → (откроется меню игры — закрой
  его) ты свободен, сеанс закрыт.
- [ ] **Кнопка ✕**: сеанс → «✕ ВЫЙТИ ИЗ ТЕРМИНАЛА» в пульте → свобода.
- [ ] **Консольная железная дверь**: сеанс → консоль `~` → `p11_dspxit` →
  сеанс закрыт («нет доступа» быть не должно — PUBLIC).
- [ ] **Чатовая дверь**: сеанс → `!выйти` в чат → сеанс закрыт.
- [ ] **READY-рукопожатие**: p11_dspdebug 1 → в сеансе внизу HUD
  «active=true … cams=N doors=N»; сев за терминал — за 3 сек заморозки
  «втихую» НЕ бывает (молчание пульта = авто-срыв с подсказкой).
- [ ] **Не сломалось**: §8.66 камеры/3-е лицо/выдача ✓, §8.67 чат полной
  строки + стрелки вкладок ✓.

### §8.67 Смоук v4.19.1 «ЛЕНТА» (все галочки = релиз)

Маркеры: `сборка 4.19.1` (стартап в консоли сервера/клиента).

- [ ] **Чат полной ширины**: открой «СВЯЗЬ», набей/получи длинное
  сообщение (2+ «столбика» раньше) → строка идёт поперёк всего журнала,
  перенос только у правого края. «Грязь убирает УБОРЩИК…» во весь ряд.
- [ ] **Ресайз**: смени разрешение → чат пересобрался, сообщения
  перенесены под новую ширину (OnScreenSizeChanged тот же).
- [ ] **Вкладки не за кадром**: F4 → АДМИН (ранг 4+) → в ряду видно ~9
  вкладок + стрелки ◀ ▶ справа; 12-я («ИВЕНТ НЕЧТО») НЕ вылезает за
  край — доезжаешь стрелкой ▶.
- [ ] **Подгон активной**: открой меню свежим (или кликни крайнюю
  вкладку) → активная вкладка всегда в кадре; «ОПОВЕЩЕНИЕ» и
  «ИВЕНТ НЕЧТО» находятся и кликаются.
- [ ] **Не сломалось**: Enter в чате отправляет (§8.63/«КОНТРАБАНДА»),
  оповещение из вкладки ОПОВЕЩЕНИЕ летит ✓, ивент-вкладка работает ✓,
  диспетчер «ГЛАЗ» (§8.66) на месте ✓.

### §8.66 Смоук v4.19.0 «ГЛАЗ» (все галочки = релиз)

Маркеры: `сборка 4.19.0` • сервер: `[POLUS-11] «ГЛАЗ»: диспетчер-камеры
Красных Орлов v4.19.0` • клиент: `[POLUS-11] «ГЛАЗ»: пульт диспетчера
v4.19.0 OK`.

- [ ] **Профа**: F4 → ОТРЯД «КРАСНЫЙ ОРЁЛ» → «Диспетчер „Красного Орла“»
  (вайтлист, 1 место) берётся вайтлистнутым; кейс ЛЕГАТ + рация + M1911A1
  в снаряге. Чужой (игрок без допуска): E по терминалу → «доступ у
  диспетчера / администрации 4+».
- [ ] **Расстановка**: 📍 «Расставить» → «🖥 Терминал диспетчера» и 2–3
  «📷 Камера „ГЛАЗ“» (при расстановке повернуть ТУДА, куда смотреть!).
  Рестарт карты — всё на месте (JSON-сейвы точек).
- [ ] **Сеанс**: диспетчером (или админом 4+) E по терминалу → тело
  застыло на пульте, экран: рамка станции «■ REC • ЦЕНТР», шапка
  «ГЛАЗ #1 (1/N)», справа меню-пульт. `A`/`D` листают камеры, при пустой
  сети — «СЕТЬ КАМЕР ПУСТА… расставь 📍 камеру».
- [ ] **3-е лицо**: `SPACE` → шапка «👁 ник — профа», вид за спиной
  бойца; `A`/`D` переключают людей, ХП/броня читаются в строке. Клики по
  списку людей в пульте делают то же. Стены камеру не съедают (трасса).
- [ ] **Выдача**: в режиме людей «➕ +25 ХП» → цели реально приросло ХП,
  у цели тост «📡 ЦЕНТР», в журнале «ГЛАЗ: …медпомощь…»; мгновенный
  повтор — «канал перезаряжается». 🔫 SA-58 → ствол у цели в руках
  (без пака EFT — фолбэк, класс написан в ответе пульта).
- [ ] **Приколы**: «📍 МАЯК ЦЕЛИ» → над целью 60 сек «🦅 ЦЕЛЬ ЦЕНТРА»
  видят орлы и админ (РККА/наука/криминал — нет), поверх маскировки.
  «📡 СИГНАЛ» → строка «📡 ЦЕНТР…» в чате у всех орлов.
- [ ] **Двери**: ЛКМ по строке двери → открылась/закрылась (статус в
  пульте поменялся), ПКМ → «🔒 БЛОК» — руками дверь не открыть; второй
  ПКМ → «🔓 свободна».
- [ ] **Срыв**: отошёл от терминала подальше → «Отошёл от терминала —
  сеанс закрыт», тело снова ходит и стреляет. Второе E по терминалу /
  E в сеансе = выход тоже.
- [ ] **Не сломалось**: §8.65 вкладка ИВЕНТ НЕЧТО ✓, §8.64 TAB без
  «наст.:» ✓, §8.61 точка захвата из тех же 📍 ролей ✓.

### §8.65 Смоук v4.18.2 «ВЕРБОВКА» (все галочки = релиз)

Маркеры: `сборка 4.18.2` • сервер: `[POLUS-11] ИВЕНТ НЕЧТО v4.18.2 «ВЕРБОВКА»`.

- [ ] **Кнопка из вкладки**: админом (ранг 4+) F4 → АДМИН → ИВЕНТ НЕЧТО →
  статус «ЗАКРЫТ …» → «ЗАПУСТИТЬ» → станция видит «[КАДРЫ] ОСОБАЯ ВАКАНСИЯ»,
  над кадровиком красная плашка, в статусе «ОТКРЫТ — окно ~90 сек».
- [ ] **Отзыв**: «ОТОЗВАТЬ» → «[КАДРЫ] Особая вакансия ОТОЗВАНА», плашка
  гаснет, статус «ЗАКРЫТ».
- [ ] **Консоль админа**: `p11_thingevent` (без 16 ранга) — включает/гасит;
  игрок без 4+ — отказ. `!ивент статус` в чате печатает состояние.
- [ ] **Взятие**: в окно — E по кадровику бойцом → тихая инфекция (~3 сек).
- [ ] **Не сломалось**: §8.64 TAB без «наст.» ✓, §8.63 оповещение ✓.

### §8.64 Смоук v4.18.1 «ГРИМ» (все галочки = релиз)

Маркеры: `сборка 4.18.1` • строка «(наст.: …)» в TAB отсутствует у всех.

- [ ] **TAB без палки**: боец в кейсе «ЛЕГАТ»/«ОБСЛУГА» (или личина) —
  в TAB только липовой позывной, ХОТЬ у админа, хоть у обычного игрока;
  суффикса «(наст.: …)» нет нигде.
- [ ] **Зеркало живо**: `p11_spies` в консоли (Глава) по-прежнему печатает,
  кто в какой липовой легенде.
- [ ] **Не сломалось**: §8.63 оповещение и 3D-голос ✓, §8.62 чат ✓.

### §8.63 Смоук v4.18.0 «РЕПРОДУКТОР» (все галочки = релиз)

Маркеры: `сборка 4.18.0` • сервер: `[POLUS-11] 3D-голос v4.18.0 «РЕПРОДУКТОР»`
и `[POLUS-11] оповещения «РЕПРОДУКТОР» v4.18.0` • `[РЕПРОДУКТОР] …` на запуск.

- [ ] **Местная речь**: два игрока в 800+ юн друг от друга на местной речи
  (рации на явных каналах/без раций) — голос НЕ слышен; подошли — слышно с
  затуханием; `p11_voiceradius 400` ужимает круг на лету.
- [ ] **«Общий» канал**: оба на «all» — дальше ~2250u тишина, рядом слышно;
  явный канал (например «Командный») у обоих — слышно и через всю карту.
- [ ] **Рация-диверсант**: включённая буря глушит эфир, разговор в лицо жив.
- [ ] **Вкладка**: F4 → АДМИН → ОПОВЕЩЕНИЕ → написал текст → кнопка → у всех
  наверху янтарная плашка 14 сек + дубль `[ОПОВЕЩЕНИЕ]` в чате + в консоли
  сервера `[РЕПРОДУКТОР] ник: текст`.
- [ ] **Консоль**: `p11_announce построение через пять минут` (клиент-Глава)
  → та же плашка; игрок без ранга 4 — молча отказано.
- [ ] **Не сломалось**: §8.62 чат/криминал ✓, §8.61 захват ✓.

### §8.62 Смоук v4.17.0 «КОНТРАБАНДА» (все галочки = релиз)

Маркеры: `сборка 4.17.0` • клиент: `[P11CHAT-СВЯЗЬ] v4.17.0 «КОНТРАБАНДА» OK`
• в логе сида: новые должности `seed_crime_*` (+фракция Криминал).

- [ ] **Enter отправляет**: Y → печать → Enter → сообщение ушло (след
  `[СВЯЗЬ→]` в консоли). Повтори 3 раза подряд и с разными каналами.
- [ ] **Чат шире**: длинное сообщение больше не ломается «столбиком».
- [ ] **Репорты справа**: вторым игроком кинь `/report тест` → у админа окно
  📨 само выехало у правого края (повторные — не чаще раз в 12 сек).
- [ ] **Уборщик**: E по грязи → 5 этапов, каждый ~1.4 сек, всё успевается.
- [ ] **Деньги/танк**: казна/донат над 100к не режет до 1М; «Бизон» = 65 000₽.
- [ ] **Криминал**: F4 → «Криминал станции» → курьер доступен сразу; в руках
  кейс «ОБСЛУГА» → ЛКМ: модель стоковой обслуги + липовой позывной (TAB и
  ник над головой — под персонал), повторный ЛКМ — маска снята.
- [ ] **TAB-меню**: TAB → ЛКМ по игроку → окно: профиль открывается в Steam
  Overlay, три кнопки копируют (вставь куда-нибудь Ctrl+V — текст совпадает).
- [ ] **Не сломалось**: §8.61 захват ✓, §8.60 чат ✓, маскировка «ЛЕГАТ» ✓.

### §8.61 Смоук v4.16.0 «ЗАХВАТ» (все галочки = релиз)

Маркеры: `сборка 4.16.0` • сервер: `[POLUS-11] точка захвата «ФЛАГ» v4.16.0`
• клиент: `[POLUS-11] ЗАХВАТ: HUD v4.16.0` • журнал сида: `eagleV416`.

- [ ] **Закупка в инвентарь**: купил АК у ларька → в руках НЕ появился,
  лежит в 🎒 (C-меню → Инвентарь); ИСПОЛЬЗОВАТЬ — берётся в руки;
  «⬇ В БАГАЖ» — кладёт из рук обратно ячейкой.
- [ ] **Поставить точку**: 📍 Расставить → 🚩 Точка захвата → встала с
  кругом и именем «А»; после рестарта на месте; `p11_flagclear` — снос всех.
- [ ] **Захват**: бойцем РККА в кругу — HUD «ЗАХВАТ: РККА %» растёт, за
  ~60 сек — объявление станции «РККА ВЗЯЛА ТОЧКУ «А»»; Орлом в кругу —
  отнимается обратно; маяк «🚩 ТОЧКА ЗАХВАТА» в курсе.
- [ ] **Бой за точку**: в круге РККА + Орёл одновременно — шкала стоит,
  HUD «БОЙ ЗА ТОЧКУ — шкала заморожена!».
- [ ] **Оклад**: ~90 сек удержания → бойцы владеющей фракции получают
  «💰 +35₽ — удержание точки».
- [ ] **Орёл вооружён**: F4 → отряд — у агента MP5, у связного UMP-45,
  оператор/резидент/командир с длинными стволами, диверсант с M700;
  в консоли сервера при первом запуске — запись `eagleV416` в логе сида.
- [ ] **Не сломалось**: §8.60 чат ✓, §8.58 !ивент ✓, нечто live ✓.

### §8.60 Смоук v4.15.4 «ОБХОД» (все галочки = релиз)

Маркеры: `сборка 4.15.4` • клиент: `[P11CHAT-СВЯЗЬ] v4.15.4 «ОБХОД» OK` •
в клиентской консоли по отправке — след `[СВЯЗЬ→] …`.

- [ ] **Печать + Enter**: Y → набрал «проверка связи» → **Enter** → строка
  ушла, чат закрылся, в журнале своё сообщение; в клиентской консоли след
  `[СВЯЗЬ→] проверка связи`. То же со второго-третьего сообщения подряд.
- [ ] **Датчик**: `p11_pchatdebug 1` → поверх чата «окно=живо open=true`,
  «фокус=DTextEntry», «мышь: рамка=true поле=true», «submit=…» после отправки;
  `p11_pchatdebug 0` гасит.
- [ ] **Канал без мыши**: `p11_chmode ooc` → плейсхолдер «OOC…», отправка
  ушла с `// `; `p11_chmode` без аргумента печатает список каналов.
- [ ] **Клики**: как §8.59 — канал по полосе выбирается, клик по чату =
  курсор в поле сразу, повторная печать без перезахода.
- [ ] **Курсор чистый**: после ESC/Enter курсора нет, панель ввода спрятана;
  двойной ESC — стандартное меню.
- [ ] **Не сломалось**: §8.59 ✓, §8.58 !ивент ✓, входящие рисуются.

### §8.59 Смоук v4.15.3 «КУРСОР» (все галочки = релиз)

Маркеры: `сборка 4.15.3` • клиент: `[P11CHAT-СВЯЗЬ] v4.15.3 «КУРСОР» OK`.

- [ ] **Клики по каналам**: Y → ЛКМ по «OOC» — чип подсветился, плейсхолдер
  «OOC: пиши сюда…», отправка уходит с `// `; то же «РЕЧЬ»/«РАЦИЯ»/«ШЁПОТ».
  Страховь без мыши: TAB/Shift+TAB крутит те же каналы.
- [ ] **Печать по клику**: Y → напечатал → кликнул мышью в журнал/полосу/
  пустое место чата → каретка В ПОЛЕ, продолжаешь печатать сразу (не нужно
  закрывать-открывать).
- [ ] **ESC/мышь чистые**: ESC закрывает (один контур или второй), курсор
  мыши после закрытия исчезает, двойной ESC открывает обычное меню.
- [ ] **Ширина**: окно шире прежнего (≈72% экрана), чипы не налезают друг
  на друга, журнал выше; худ ХП/денег по-прежнему ниже чата.
- [ ] **Не сломалось**: входящие сообщения рисуются (личина в именах),
  §8.58 !ивент ✓.

### §8.58 Смоук v4.15.2 «НАБОР» (все галочки = релиз)

Маркеры: `сборка 4.15.2` • консоль сервера после `p11_thingevent on`:
«[КАДРЫ] ОСОБАЯ ВАКАНСИЯ открыта…».

- [ ] **Консоль запуск**: `p11_thingevent on` (Глава 16/RCON) → объявление
  в чат станции «Поступила ОСОБАЯ ВАКАНСИЯ…», красная плашка над кадровиком,
  `p11_offerdiag` — «окно открыто»; повторный `on` — честное «уже открыт».
- [ ] **Боец берёт**: E по кадровику в окно → тихо заражён; окно закрыто,
  активное нечто одно (§8.47-вакансия жива).
- [ ] **Отмена**: `p11_thingevent off` → «вакансия ОТОЗВАНА» всем, плашка
  погасла, журнал пишет кто отозвал.
- [ ] **Чат-дверь админа (4+)**: «!ивент» в чат → окно открылось; «!ивент
  стоп» → закрыло; «!ивент статус» → ответ состояния; боец-не-админ пишет
  «!ивент» → вежливый отказ рангом; не-админские «!ивент» не гуляет как OOC.
- [ ] **Статус**: `p11_thingevent status` — открыт/закрыт + период плановых
  волн 900–1800 сек.
- [ ] **Не сломалось**: плановые волны на новом расписании сами идут,
  p11_offer-тумблер жив, §8.57 ЗиС-5 ✓.

### §8.57 Смоук v4.15.1 «ТРЁХТОНКА» (все галочки = релиз)

Маркер: `сборка 4.15.1` • витрина гаража — карточка «ЗиС-5 «Трёхтонка» (1941)».

- [ ] **Карточка витрины**: Гараж-мастер → в списке между «ГАЗ «Снабжение»»
  и «Як-2» — «ЗиС-5 «Трёхтонка» (1941) · 11 000₽ · КУПИТЬ» (пак не стоит —
  «НЕТ ПАКА», денег не списывает — проверь сначала снятый пак, если есть).
- [ ] **Покупка**: 11 000₽ на кошельке → КУПИТЬ → грузовик на площадке у
  торговца, деньги списались, в журнале «гараж: ЗиС-5»; старый транспорт
  бойца утилизирован (правило «одна машина»).
- [ ] **Едет и грузит**: сел за руль → LVS-физика едет; кабина греет
  (правило «в транспорте не замерзает» работает).
- [ ] **Не сломалось**: «Бизон»/«Ворошиловец»/ГАЗы/Як-2 покупаются как
  раньше; §8.56 очаг/лутницы/чат ✓.

### §8.56 Смоук v4.15.0 «УГЛИ» (все галочки = релиз)

Маркеры: `сборка 4.15.0` • клиентская консоль: `[P11CHAT-СВЯЗЬ] v4.15.0 «УГЛИ» OK`
• Q-меню → энтити ПОЛЮС-11: «Буржуйка «УГЛИ»», «Продовольственный ящик»,
«Армейский контейнер».

- [ ] **Чат открывается и ПИШЕТСЯ**: Y → строка ввода видна сразу, курсор
  мигает, кириллица/латиница печатаются; Enter уходит (сообщение в журнале).
- [ ] **Каналы кликаются**: ЛКМ по «РАЦИЯ» → чип подсвечен, плейсхолдер
  сменился, отправка ушла с префиксом `/r `; TAB/Shift+TAB крутят каналы.
- [ ] **ESC закрывает**: один ESC — чат схлопнулся БЕЗ открытия меню игры;
  повторный ESC — обычное меню. Мышь после закрытия свободна (курсора нет).
- [ ] **Чат выше худов**: на 1080p верх/низ чата не пересекают худ ХП и
  золотой чип денег; приёмы: шрифт крупнее, журнал выше, счётчик пропущенных
  лежит над окном.
- [ ] **ОЧАГ**: поставил 📍 → буржуйка (или `p11_lootspawn hearth 1`) →
  купил 2 канистры → E дважды → «очаг горит ~480+ сек», вокруг тепло: худ
  мороза ползёт ВВЕРХ рядом с печью (метка «греешься у огня» не обязательна —
  цифра тепла растёт); вдали от печи мороз донимает как раньше. Спирт: E —
  +90 сек. Пустые руки — честный перечень чего не хватает. Конец огня:
  свет/треск стихают, табличка «холодная».
- [ ] **ЛУТНИЦЫ**: `p11_lootspawn food 5` и `arm 4` (или 📍) → обыск E:
  прод — тушёнка/паёк/спирт + рубли; армейский — патроны/(иногда боекомплект);
  обе темнеют и пишут таймер «пополнение через ~N сек», маяки GPS 🥫/🪖 живы;
  через 5/7 мин снова полные и светлые.
- [ ] **Не сломалось**: старые 6 лутниц/верстак лутаются, задача «обыск»
  тикает; §8.50/§8.56-чат при том же FADE и личине имени; §8.55 авто/деньги ✓.

### §8.55 Смоук v4.14.5 «ТИШИНА» (все галочки = релиз)

Маркеры: `сборка 4.14.5` • консоль `p11_offerdiag` — «следующая плановая»
считает от 900–1800 сек.

- [ ] **Ивент реже**: `p11_offerdiag` → следующая вакансия через 15–30 мин
  (не 4–8); ручной `p11_offer` по-прежнему форсит окно для ивента; взятая
  вакансия/истёкшее окно сдвигают следующую на те же 15–30 мин.
- [ ] **Деньги в гараже — хирургический случай**: у бойца в `economy.json`
  30000₽ (заглянуть в data или дать `p11_kaznagive money ник 30000`) → к
  Гараж-мастеру → КУПИТЬ ГАЗ-Снабжение 8500 → **списывает и выдаёт**, без
  «нет денег»; если память отставала — в консоли сервера «ЭКОНОМИКА:
  кошелёк дотянут из сейва …».
- [ ] **Витрины совпадают**: в меню гаража вверху справа та же сумма, что в
  золотом чипе HUD и в 🎒 (max-согласование).
- [ ] **Синк лечит**: открыть 🎒 или ларёк — любой рассинхрон клиентских
  денег гаснет сразу (NWInt переписывается серверной правдой каждый синк).
- [ ] **Не сломалось**: покупки в ларьке/магазине ПФ списывают как раньше;
  §8.54 «БАГАЖ» укладка ствола ✓; смерть/смена профы снимают нечто ✓
  (§8.53), казна ✓ (§8.52).

### §8.54 Смоук v4.14.4 «БАГАЖ» (все галочки = релиз)

Маркеры: `сборка 4.14.4` • 🎒 C-меню → внизу окна кнопка «⬇ В БАГАЖ: …».

- [ ] **Укладка боевого ствола**: купил АК-74 в ларьке (в руках) → 🎒 →
  кнопка «⬇ В БАГАЖ: АК-74» → ствол из рук ушёл, в списке «АК-74 ×N»,
  тост «убран в багаж», в журнале сервера строка «убрал … в багаж».
- [ ] **Возврат**: «ИСПОЛЬЗОВАТЬ» у строки → снова в руках, ячейка −1.
- [ ] **Ярлык живой**: достал карманный ствол → подпись меняется на его имя;
  пустые руки → «в руках пусто»; когти нечто/физган → «не лезет (не из
  ларька)», сервер отказом подтверждает (ничего не пропадает!).
- [ ] **Наследие**: старый скриптовый РПД (если остался у кого в руках)
  складывается в ячейку «РПД» — потом достаётся уже ARC9 EFT.
- [ ] **Не сломалось**: список предметов прокручивается, кнопка
  🛠 МАСТЕРСКАЯ на месте и открывает крафт; §8.53 (РПД ARC9 / ДС / антиспрей
  / смерть-профа снимают нечто / HUD мутаций) ✓; ларёк и сейф (акты 2/3/4)
  работают как раньше.

### §8.53 Смоук v4.14.3 «ЗАРЯД» (все галочки = релиз)

Маркеры: `сборка 4.14.3` • консоль сервера: `НЕЧТО «КОРЕНЬ» v4.14.3 «ЗАРЯД»`
и `ЗАПРЕТ СПРЕЕВ v4.14.3 «ЗАРЯД»` • F6-донат без плашки «ХОЗЯИНУ».

- [ ] **ARC9 EFT РПД у пулемётчика**: встал в профу «Пулемётчик РККА» →
  в руках `arc9_eft_rpd` (проверка: консоль `status`, ствол с прицелом/
  тюнингом ARC9). Пак ARC9 EFT не стоит — ствола нет (клади аддон!).
- [ ] **ARC9 EFT РПД за рубли/ПФ**: ларёк → «РПД» 12500₽ → СРАЗУ В РУКИ;
  донат F6 → «РПД — прямо в инвентарь» 60 ПФ → 🎒 → ИСПОЛЬЗОВАТЬ →
  в руках тот же ARC9-ствол. Без пака — честные отказы, деньги не ест.
- [ ] **ДС-канал вшит**: F6 → кнопки пакетов 500₽/900₽/1500₽ открывают
  discord.gg/kdNXgaetC; надпись «ХОЗЯИНУ: вставь инвайт ДС» ИСЧЕЗЛА и
  сверху, и в окне талона.
- [ ] **Спреев нет**: консоль `impulse 201` (или кнопка спрея) — декаль
  НЕ ложится ни у бойца, ни у админа; стены чистые.
- [ ] **Смерть снимает нечто (стоящий режим)**: заражённый умер →
  на респавне тост «смерть выпустила паразита», когтей/личины нет,
  `p11_thingroot` не показывает бойца в носителях.
- [ ] **Смена профы снимает нечто (стоящий режим)**: у кадровика на
  новую должность → тост «смена должности вытряхнула паразита», стал
  человеком. Хочешь старый вечный режим: `p11_thing_deathdrop 0` +
  `p11_thing_jobdrop 0` (после рестарта «ОНО ОСТАЛОСЬ» вернётся).
- [ ] **HUD мутаций чистый**: у активного нечто в углу «МУТАЦИИ ТВАРИ»
  БЕЗ квадратов □□, строка «жертв: N» справа не наезжает на заголовок,
  «до мутации: 0/3» и список тиров читаются.
- [ ] **Не сломалось**: §8.52 казна (ростер/оффлайн/списания) ✓,
  §8.50 чат «СВЯЗЬ» ✓, §8.47–8.49 когти/400 ХП/auto-поглощение ✓
  (поглощение теперь — до первой смерти/смены профы).

### §8.52 Смоук v4.14.2 «КАЗНА» (все галочки = релиз)

Маркеры: `сборка 4.14.2` • консоль сервера: `[POLUS-11] КАЗНА СТАНЦИИ v4.14.2`
и `НЕЧТО «КОРЕНЬ» v4.14.2 «КАЗНА»` • клиентская: `КАЗНА КЛИЕНТ v4.14.2`.

- [ ] **Ростер трёх валют**: `/menu` → «💠 ПОТОК» → «🛠 КАЗНА СТАНЦИИ» →
  окно p11_kazna со списком бойцов (💠/₽/⏱ по каждому); клик по валюте ⇒
  строка подсвечена, «ЦЕЛЬ» заполнена → +100 → «ВЫДАТЬ» ⇒ тост обоим,
  цифра в ростере обновилась, в консоли сервера строка «КАЗНА: …».
  Боец рангом ниже 4: окно пустое/молчит, сервер отказал честно.
- [ ] **Оффлайн-строки (Глава 16)**: три строки (💠 ПФ / ₽ ДЕНЬГИ / ⏱ СТАЖ)
  с целью SteamID64 ушедшего → «ВЫДАТЬ» ⇒ чат «Команда ушла:
  p11_kaznagive …», журнал «КАЗНА: … ОФФЛАЙН»; при входе бойца —
  зачислено. Ниже Главы рангом — замок консоли отвечает отказом.
- [ ] **Списание**: сумма с минусом → баланс уменьшается, время кладётся в
  `playtime.json`, рубли — в `economy.json` (файлы не тронуты обновлением).
- [ ] **Диагностика**: `p11_kaznalist` — таблица онлайн-балансов.
- [ ] **Дверь выхода №1 (пульт)**: заражённому — R → «🩸 ИЗГНАТЬ ПАРАЗИТА»,
  один клик = чат-подсказка, ВТОРОЙ клик = паразит снят (когти ушли,
  ник/облик свои, «ты снова человек»), журнал «САМО-ИЗГНАНИЕ ПАРАЗИТА».
  На инкубации (заражён, не активен) кнопку видно тоже — работает.
- [ ] **Дверь выхода №2 (консоль)**: `p11_thingleave` (публичная) — то же
  самое; незаражённому честно «изгонять нечего».
- [ ] **Режим «до рестарта» стоит** (рубильники 0): убил нечто → на респавне
  «ОНО ОСТАЛОСЬ»; сменил профу → личина/флаги на месте (§8.47–8.49 живы).
- [ ] **Рубильники**: `p11_thing_deathdrop 1` → смерть нечто СНИМЕТ
  (тост «смерть выпустила паразита»); `p11_thing_jobdrop 1` → то же от
  смены профы; вернуть `0` — режим «до рестарта».
- [ ] **Сборка клиента**: на выделенном сервере чат «СВЯЗЬ» открывается у
  ПОДКЛЮЧЁННОГО бойца (клиент скачал p11_cl_pchat.lua — §8.50 жив),
  окно казны тоже открывается.

### §8.51 Смоук v4.14.1 «ПОТОК» (все галочки = релиз)

Маркеры: `сборка 4.14.1` • `/menu` → 10 вкладок, четвёртая — «💠 ПОТОК».

- [ ] **Вкладка на месте**: `/menu` → «💠 ПОТОК» (между ДЕЙСТВИЯ и
  ДОЛЖНОСТИ) открывается с ранга Administrator (4)+; у бойца ниже —
  серая, клик говорит честный отказ с рангом.
- [ ] **Онлайн-выдача**: кнопка «ОТКРЫТЬ УТИЛИТ-МЕНЮ…» → окно ростера
  (та же дверь, что F6 и p11_utils): +100 бойцу → тост у него, строка
  в журнале сервера «ФЛЮКС +100: …».
- [ ] **Оффлайн/магазин**: поле SteamID64 + сумма → «ВЫДАТЬ ПФ» →
  чат подтверждает «Команда ушла: p11_fluxgive …» (работает Главе 16 —
  замок консоли; ниже рангом — замок ответит «только Глава»). Ушедшему
  ПФ зачислится при входе.
- [ ] **Справка**: блок «Где что лежит» читается (сейв flux.json,
  журнал, пакеты F6, docs/DONATE.md).
- [ ] **Не сломалось**: все 10 вкладок влазят в окно (самая правая —
  ВАЙТЛИСТ), F6 → 🛠 УТИЛИТЫ ВЫДАЧИ открывает то же окно,
  §8.50 (чат «СВЯЗЬ») ✓.

### §8.50 Смоук v4.14.0 «СВЯЗЬ» (все галочки = релиз)

Маркеры: `сборка 4.14.0` • клиентская консоль: `[P11CHAT-СВЯЗЬ] v4.14.0 OK —
свой чат станции…` • чат открывается клавишей Y: рама с полосой каналов.

- [ ] **Свой чат жив**: зашёл → внизу слева через ~1 сек журнал «СВЯЗЬ».
  В консоли клиента НЕТ красного; BonChat.frame отсутствует (он спит).
  Y или Enter → открывается НАШЕ окно (полоса каналов сверху ввода).
- [ ] **Полоса каналов**: клик «РАЦИЯ» → чип подсветился, набранный текст
  НЕ стёрся и префикс в текст не подставился (только при отправке ушло
  «/r …»). TAB в поле → каналы крутятся по кругу, Shift+TAB — назад.
  Стрелки ↑/↓ — прошлые отправленные строки.
- [ ] **Каналы работают**: РЕЧЬ слышна рядом (имя цвета профы + « говорит:»),
  OOC всем серым [OOC], /шепот у уха, /крик ярким, ME фиолетовым «• имя …»,
  РЕПОРТ уходит админам. Рация в журнале — янтарем. Роутер сервера не
  тронут: радиусы as is.
- [ ] **Личина цела**: тварь в чужой шкуре пишет — имя и цвет чужой
  должности (как в TAB). Системные строки «зашёл на станцию» видны в
  журнале серо-зелёным.
- [ ] **Затухание и пропущенные**: журнал без нового слова гаснет за ~10
  сек; прилетело при закрытом — внизу «ПРОПУЩЕНО: N — чат: клавиша Y»,
  звонок слышен. Колёсико крутит историю, бегунок справа.
- [ ] **Рубильник**: `p11_ownchat 0` → окно «СВЯЗИ» исчезло, BonChat ожил
  (его полоса v4.9.2 тоже на месте). `p11_ownchat 1` → вернулся свой.
  `p11_pchat` печатает «окно=ЖИВО». `p11_chatfix` показывает оба фронта.
- [ ] **Не сломалось**: §8.47 (нечто), §8.48/8.49 (400 ХП), полоса
  над BonChat в режиме p11_ownchat 0 работает как раньше.

### §8.49 Смоук v4.13.2 «КРЕПЬ» (все галочки = релиз)

Маркеры: `сборка 4.13.2` • `[POLUS-11] НЕЧТО «КОРЕНЬ» v4.13.2 «КРЕПЬ»: …
СВЕП НЕЧТО АВТОМАТОМ ДАЁТ 400 ХП (достаточно самих когтей; тюнер
p11_thinghp) …`.

- [ ] **Достаточно когтей**: дай бойцу `give weapon_polus11_thing`
  (хоть без заражения) → ≤1 сек максимум 400, ХП полные, тост
  «Плоть уплотнилась…». `p11_thingroot`: буст=1.
- [ ] **Картонки нет**: постреляй носителя из автомата — ХП отнимаются,
  но максимум НЕ падает (пули ×0.35), убить без огня — долго; огнемёт
  плавит (×3) — это счётчик по замыслу.
- [ ] **Тюнер**: `p11_thinghp` → «сейчас: 400»; `p11_thinghp 600` →
  «= 600, носители перевыставлены следующим тиком»; через сек максимум
  600 (хил не халявный — обрезка сверху только при снятии). Верни
  `p11_thinghp 400`.
- [ ] **Честность не тронута**: забери когти (`strip weapon_polus11_thing`)
  → тело вернулось к ХП профы. Антидот/админ-излечение — то же.
  §8.48 (респавн/профа/брут 470) по-прежнему ✓.

### §8.48 Смоук v4.13.1 «ТУША» (все галочки = релиз)

Маркеры: `сборка 4.13.1` • `[POLUS-11] НЕЧТО «КОРЕНЬ» v4.13.1 «ТУША»: …
СВЕП НЕЧТО АВТОМАТОМ ДАЁТ 400 ХП …`.

- [ ] **Когти = 400 ХП**: активное нечто с когтями → ≤2 сек после
  пробуждения максимум 400 и ХП полные, тост «Плоть уплотнилась —
  тело нечто: 400 ХП.». Проверка: `p11_thingroot` — ХП=400/400(буст=1).
- [ ] **Буст держится в бою**: получи 50 урона когтями в руках →
  максимум остаётся 400 (хил — как у людей: медики/паёк/поглощение +25).
- [ ] **Поглотитель**: возьми форму «Поглотитель», явись →
  максимум 470 (400+70), скорость занижена как раньше; спрятался → 400.
- [ ] **Честное снятие**: антидот медика / админ-пульт «ИЗЛЕЧИТЬ» →
  максимум возвращается к ХП профы (штурмовик 125), ХП обрезаны сверху.
- [ ] **Респавн/профа**: умри будучи нечто → на респавне максимум = ХП
  профы, через 1–2 сек (когти вернулись) снова 400. Смени профу —
  максимум по новой должности, затем снова 400.
- [ ] **Не сломалось**: поглощение лечит +25 до 400, задачи нечто идут,
  §8.47 (неумирает/маскировка/личина в чате и рации) по-прежнему ✓.

### §8.47 Смоук v4.13.0 «КОРЕНЬ» (все галочки = релиз)

Маркеры: `сборка 4.13.0` • `[POLUS-11] НЕЧТО «КОРЕНЬ» v4.13.0: заражение
НЕУБИВАЕМО до рестарта — …диагностика p11_thingroot`.

- [ ] **Нечто переживает смерть**: зарази бойца (вакансия у кадровика /
  укол / !пульт), подожди пробуждения → пусть УМРЁТ любым способом →
  на респавне: сообщение «ОНО ОСТАЛОСЬ с тобой…», когти на месте
  (или вернулись за ≤2 сек), R — пульт тела жив. `p11_thingroot` из
  сервер-консоли: у бойца актив=1, когти=true.
- [ ] **Нечто переживает смену профы**: тем же активным нечто подойди
  к кадровику, возьми ДРУГУЮ должность → тосты «смена должности его не
  выпускает»/«ОНО осталось с тобой», заражение на месте
  (`p11_thingtest`: заражён=1, активен=1, когти в снаряге=1).
  Если нечто было В ЧУЖОЙ ЛИЧИНЕ — после смены профы оно выглядит
  той же украденной внешностью (личина натянулась обратно, позывной
  и документ — чужие). В консоли сервера строки «НЕЧТО·КОРЕНЬ: смена
  должности при заражении…».
- [ ] **Полная маскировка — чат**: тварь сожрала бойца когтями
  (авто-поглощение) → написала что-то в обычный чат → у ОСТАЛЬНЫХ
  отправитель = УКРАДЕННЫЙ позывной жертвы, цвет = цвет должности
  жертвы. (У самой твари её сообщение тоже подписано личиной.)
- [ ] **Полная маскировка — рация**: тварь в личине говорит в рацию
  → все слышат «[Рация:…] <личина>: …». В консоли сервера честная
  строка «РАЦИЯ […] <настоящий ник> (в эфире как «<личина>»): …».
- [ ] **Осмотр (документы/TAB/ники)**: TAB у твари — украденные имя,
  должность и КОД ДОКУМЕНТА жертвы (админам в скобках видно «наст.:» —
  это норма), ник над головой — личина.
- [ ] **Честное снятие**: антидот медика / админ-пульт «ИЗЛЕЧИТЬ» →
  после смерти вылеченного он УЖЕ не воскресает нечто
  (`p11_thingroot` его больше не показывает).
- [ ] **Не сломалось старое**: авто-поглощение (труп исчезает за
  0.35 сек), «снять личину» из пульта тела возвращает СВОЁ лицо,
  вакансия у кадровика берётся, блэкаут/буря с пульта работают
  (§8.46, §8.45).

### §8.46 Смоук v4.12.2 «ЭФИР» (все галочки = релиз)

Маркеры: `сборка 4.12.2` • `[P11POI] v4.12.2 «ЭФИР» OK — мир чист по
умолчанию` • у клиента в консоли: `фронтенд чата: BonChat … подключён
клиенту ✔` (водной строки «Chat «ПОЛЮС-11 · ЭФИР» загружен» в окне чата).

- [ ] **Чат-страховка**: `~` клиент → `bonchat_enable 0` → ввод/ввод —
  ванильный чат. Через ≤9 сек после (пере)захода страховка вернула 1:
  окно «ПОЛЮС-11 · ЭФИР» само ожило + уведомление. `p11_chatfix` — статус
  «панель ЖИВА». (Если у тебя чата не было ДО этой версии — обновись и
  перезайди: вернётся сам.)
- [ ] **Спавн из меню — с подтверждением пути**: `/menu` → СПАВНЫ →
  фракция РККА → «ПОСТАВИТЬ ЗДЕСЬ» → в клиентском чате СРАЗУ
  «[ПЛАЦ] … запрос ушёл на сервер», в консоли СЕРВЕРА — строка
  «ПЛАЦ-запрос: ник act=33», далее тост «поставлена ЗДЕСЬ» + ✓ Проверка
  записи. Нет строки act= — клиентская сборка старая (перекачай zip
  полностью: удалить gamemodes/polus → распаковать заново).
- [ ] **Халява**: та же вкладка → «⚡ ВСЁ ЗДЕСЬ» → «✓ Расставил: фракций N,
  проф M» + ✓ Проверка + куб на 8 сек. Умри/смени профу — спавнит на плацу
  по приоритету профа → фракция → гарнизон.
- [ ] **GPS**: мир при входе ЧИСТЫЙ (без отметок). C → «🧭 GPS-КУРС» →
  список точек с метрами → клик по «ЛАРЁК» → один маяк 🧭 ведёт; подходишь
  на 14 м — «ПРИБЫЛ», маяк снялся сам. «СТОП НАВОДКА» снимает досрочно.
  `p11_poi 1` — все маяки как раньше (до перезахода), `p11_poi` тумблит.
  Пустая лутница целью → курс снялся со строкой «цель пуста».
- [ ] **Не сломалось старое**: полоса каналов над чатом на месте
  (РЕЧЬ/OOC/РАЦИЯ…), p11_arrival here/all по консоли работают (§8.45).

### §8.45 Смоук v4.12.1 «ПЛАЦ» (все галочки = релиз)

Маркеры: `сборка 4.12.1` в статусе; корректная постановка точек из
админ-меню (раньше act 33/37 обрезались 5-битным UInt в чужие действия).

- [ ] **Меню → спавн фракции**: `/menu` (админ) → вкладка зон → комбо фракции
  (напр. РККА) → «ПОСТАВИТЬ ЗДЕСЬ» → НЕ тишина, а: куб-ориентир 5 сек +
  «Зона прибытия фракции «РККА» поставлена ЗДЕСЬ» + **✓ Проверка записи**
  (зон фракций = 1…). Никто не арестован (раньше act 33 летел в арест!).
- [ ] **Меню → спавн профы**: комбо должности (напр. Пулемётчик) →
  «ПОСТАВИТЬ ЗДЕСЬ» → тост + ✓ Проверка + куб. Никто не уволен (раньше
  act 37 летел в увольнение!).
- [ ] **Точка работает**: встань на другую профу-пустышку/перезайди →
  `p11_spawntest` телепортирует на поставленную точку (РККА → казармы).
  Рестарт карты — точки выжили (`p11_arrival marks` показывает кубики).
- [ ] **Грузовик и список**: «ЗАСПАВНИТЬ» грузовик колонны — появился/
  честный отказ про LVS-пак; «ЧТО УЖЕ РАССТАВЛЕНО?» — список в чат,
  не фантомное действие.
- [ ] **Консольный путь не сломан**: `p11_arrival here` и `p11_arrival all`
  — ✓ Проверка записи + кубики как раньше.

### §8.44 Смоук v4.12.0 «ОТБОЙ» (все галочки = релиз)

Маркеры: `сборка 4.12.0` • `[POLUS-11] энергосистема ВЫВЕДЕНА из игры v4.12.0 «ОТБОЙ»` •
`[POLUS-11] ЛУТ «ОТБОЙ» v4.12.0` • `[P11POI] v4.12.0 «ОТБОЙ» OK`

- [ ] **Генераторов нет — НАГЛУХО**: на карте/в Q-меню качнуть генератор —
  он пропадает мгновенно (в логе «ОТБОЙ: попытка поставить генератор — снята»).
  📍 «Расставить» — строки «⚡ Генератор» НЕТ (меню теперь со скроллом).
  Рестарт карты — генератор не воскресает (persist вычищен). Свет горит
  всегда; пульт Нечто: блэкаут включается как раньше, кнопка «долить
  генераторы» честно отвечает «машинное закрыто».
- [ ] **Задачи механика**: F4 → Техник-механик → в делах смены НИЧЕГО про
  генератор; вместо этого «ТО станции: собери на верстаке ×3» и «добудь
  запчасти из лутниц ×2». Обыск любой лутницы → прогресс ×1; сборка
  самокрута → прогресс ×1. Инженер/грузчик/повар/персонал — тоже без
  генераторных дел.
- [ ] **Стол крови возвращён**: 📍 → «🩸 Стол анализа крови» и
  «🧪 Лабораторный стол» — ставятся с E-интерактивом, после рестарта
  на месте. В Q-меню → Энтити → ПОЛЮС-11 — видны всем (не только админу).
- [ ] **Новые лутницы**: `p11_lootspawn med 5`, `p11_lootspawn mil 4`,
  `p11_lootspawn tech 8` — медшкаф/боевой ящик/груда лома встали, маяки
  гаснут при обыске. Медшкаф: бинты/ампулы (иногда шприц/медкейс). Боевой:
  пачки патронов → 🎒 → ИСПОЛЬЗОВАТЬ → +боекомплект. Груда: лом ×2-3.
  `p11_lootclear all` — вычистило все 7 ролей (включая старые crate/barrel/
  cache/table).
- [ ] **Сохранение**: все новые точки переживают рестарт карты; задачи
  смены прогрессируют; верстак с 12 рецептами работает как в §8.43.
- [ ] **Не сломалось старое**: ларёк/сейф/гараж/терминал/кухня ставятся
  через 📍 (скролл листается), покупка ствола — сразу в руки (§8.43),
  тест крови на столе «КРОВЬ-2» проходит и выдаёт вердикт.

### §8.43 Смоук v4.11.0 «КУЗНЯ» (все галочки = релиз)

Маркеры: `сборка 4.11.0` • `[POLUS-11] ЛУТ «КУЗНЯ» v4.11.0` •
`[POLUS-11] кустарная мастерская v4.11.0 «КУЗНЯ»: 12 рецептов` •
`[P11CRAFT] v4.11.0 «КУЗНЯ» OK` • `[P11POI] v4.11.0 «КУЗНЯ» OK`

- [ ] **Покупка → в руки**: ВСЯКОЙ профой (стрелок, повар, медсестра — без
  разницы) купить у ларька АКС-74У и МР-43 — каждый ствол СРАЗУ в руках,
  тост «сразу в руки», в инвентаре НЕ дублируется. Повторная покупка того же
  АКС — «дубликат ушёл на склад». Паёк/рация/медкейс — по-старому в 🎒.
  Игрок-Нечто (активная тварь): купил дробовик — ушло на склад, в лапы не
  выдалось (личина при желании достанет из 🎒 сама).
- [ ] **Ящик лома**: из консоли СВОЕГО клиента `p11_lootspawn crate 12`
  (ранг 16) — ящики появились вокруг, отчёт «Рассыпано». E по ящику →
  тост с добычей (металлолом и др.), ящик потемнел, маяк 📦 погас.
  Повторный E → «пусто · пополнение через ~N сек». Через 4 мин — светлый
  и снова полный (маяк вернулся).
- [ ] **Бочка и тайник**: `p11_lootspawn barrel 6`, `p11_lootspawn cache 4`.
  Бочка даёт соляру/спирт; тайник — рубли+запчасти (периодически ампула,
  УКОЛ-С, рация, очень редко +5 ПФ). Тайник пустеет на 10 мин.
- [ ] **Верстак**: C-меню → 📍 «Расставить» → «🛠 Кустарный верстак» (или
  `p11_lootspawn table 1`). E по столу — открылась МАСТЕРСКАЯ со **ВСЕМИ
  12 рецептами** (в конце: самокрут 9×18, ПП-патроны, винтовочно-автоматные,
  картечь, скальпель, УКОЛ-С). Собрать «Самокрут 9×18» из лом+запчасти →
  пачка в 🎒 → ИСПОЛЬЗОВАТЬ → +60 пистолетных. Собрать «УКОЛ-С» →
  энтити-инъектор используется из инвентаря как в аптечке.
- [ ] **Маяки**: над верстаком «🛠 ВЕРСТАК · N м»; над полным ящиком/бочкой/
  тайником маяк есть, над обобранным — нет (до пополнения).
- [ ] **Сохранение**: `p11_lootspawn crate 5` + верстак через 📍 → рестарт
  карты (или cleanup) — всё стоит на местах. `p11_lootclear crate` — ящики
  исчезли и после рестарта не вернулись; `p11_lootclear all` — вычистило всё.
- [ ] **Не сломалось старое**: ларёк/сейф/гараж расставляются из 📍 как
  раньше; `!крафт` и кнопка 🎒→🛠 открывают те же 12 рецептов; крафт из
  инвентаря работает БЕЗ верстака (верстак — тело, не требование).

### §8.42 Смоук v4.10.0 «ГАРАЖ» (все галочки = релиз)

Маркеры: `сборка 4.10.0` • `[POLUS-11] гараж «ПОЛЮС-АВТО» v4.10.0` •
`[POLUS-11] НЕЧТО «ЛИЧИНА 3.0» v4.10.0` • `[P11POI] v4.10.0` •
`[P11CRAFT] v4.10.0` • `[POLUS-11] донат-валюта «ПОЛЮС-ФЛЮКС» v4.10.0`

- [ ] **Гараж**: админ → C-меню → 📍 «Расставить» → «🚗 Гараж (ПОЛЮС-АВТО)» →
  торговец стоит, ОЖИВАЕТ (стойка движется, не T-pose), надпись «ГАРАЖ
  «ПОЛЮС-АВТО»». `p11_garagediag` — торговец №1 стоит, классы LVS:
  какие паки есть — список ✔/✖.
- [ ] **Покупка машины**: E по торговцу → окно с 5 машинами → «КУПИТЬ»
  (денег хватает) → списано, транспорт стоит рядом, сесть [E] у дверцы.
  Вторая покупка утилизирует первую (уведомление «старый утилизирован»).
  Класса LVS нет → честный отказ «пака нет», деньги НЕ списаны.
  Як-2 без должности лётчика → отказ «допуск в небо».
- [ ] **Лётчик/Водитель в F4**: РККА → «Лётчик РККА» (модели комбинезон/
  шинель, до 2 мест), ПЕРСОНАЛ → «Водитель» (до 3 мест). Лётчик берёт Як-2.
- [ ] **Анимации**: ларёк и кадровик дышат/шевелятся (не T-pose) и у нового.
- [ ] **Мороз**: `p11_coldrate` без аргумента в консоли сервера — множитель
  0.55; выйти в метель — тепло уходит ~1.2/с (не ~2.2/с).
- [ ] **Крафт**: 🎒 инвентарь → кнопка «🛠 МАСТЕРСКАЯ» (или `!крафт`) →
  купить в ларьке «брезент+спирт» → собрать «Медкейс» — расходники списаны,
  изделие в инвентаре. Не хватает → подсказка «нужно N, есть M».
- [ ] **ФЛЮКС**: F6 → шапка «💠 ПФ». Админ rank≥4: `p11_utils` → ростер,
  +100 себе → баланс вырос; F6 → купить «Термос полярника» за 15 ПФ —
  тепло 100 + пайки ×3, списано. Кнопка «КУПИТЬ 500₽ → ДС» открывает ДС
  (инвайт вставлен в DONATE_URL — иначе красная строка-подсказка).
  Консоль сервера: `p11_fluxgive 76561198000000000 50` — начислилось оффлайн.
- [ ] **Маяки**: подписи «🏪 ЛАРЁК · N м», «🚗 ГАРАЖ · N м» в мире; `p11_poi`
  выключает, повторный — включает.
- [ ] **Нечто «ЛИЧИНА 3.0»** (корень заявки): админ открыл вакансию `p11_offer`,
  игрок взял (!вакансия) → через ~5 сек когти; [TK] в консоли сервера по шагам.
  Убей человека КОГТЯМИ → через 0.35 сек труп исчез, охотник СТАЛ жертвой
  (другая модель+ник в TAB) + уведомление в чат. R → пульт тела: кнопка
  «СНЯТЬ ЛИЧИНУ» возвращает себя; «ЯВИТЬ/СПРЯТАТЬ МОНСТРА» меняет облик;
  «ДИАГНОСТИКА» печатает состояние в чат. `p11_thingtest` в консоли игрока —
  9 строк статуса. Когти не теряются: страховь перевыдаёт (тост «когти вернулись»).

### §8.41 Смоук v4.9.3 «ГРОШ» (все галочки = релиз)

Маркеры: `сборка 4.9.3` • при любом репорте в консоли сервера строка
`РЕПОРТ #N от …` • `[P11-OFFER] v4.9.3 «ГРОШ»: кнопка «ВЗЯТЬ ОСОБУЮ
ВАКАНСИЮ» в F4…`

1. [ ] Ларёк: раздел позиций с «Патронами пистолетными x60 — 350₽»,
   «Картечь 12к — 400₽», «Боекомплект к оружию в руках — 600₽».
   Купить/использовать с РПД в руках → +150 патронов (2 диска), звук
   ammo_pickup, предмет из 🎒 списался.
2. [ ] `/report привет` → сразу «Твой репорт #N создан…» + в консоли
   сервера строка; админская глазастость: серверный сhat-дубль
   «[РЕПОРТЫ] Новый репорт #…». `/репорты` открывает окно со строчкой.
3. [ ] Нечто-вакансия (`p11_offer` от админа): красная плашка есть,
   в F4 внизу — пульсирующая красная «ВЗЯТЬ ОСОБУЮ ВАКАНСИЮ — N сек».
   Игрок жмёт её → «Кадровик отводит взгляд…», через ~3 сек — руки
   Нечто (weapon_polus11_thing). Вакансия закрыта у всех. Страховки
   тоже: `!вакансия`, `p11_takeoffer`, E по кадровику. Вне окна тот же
   трек даёт текст «Особой вакансии сейчас НЕТ…».
4. [ ] F6 → картинка цен на кнопках (500₽/900₽/1500₽). Инвайт вставлен
   (`DONATE_URL`) → клик открывает ДС; не вставлен — честное «хозяин
   ещё не вставил инвайт». Талон внизу работает как раньше.

### §8.40 Смоук v4.9.2 «ПРИЁМ» (все галочки = релиз)

Маркеры: `сборка 4.9.2` • `донат-мост v4.9.2 «ПРИЁМ» загружен` •
`3D-голос v4.9.2 «ПРИЁМ»` • `переключатель каналов чата v4.9.2
«ПРИЁМ»` • при входе науки: в снаряге рация.

1. [ ] Встать на плац (Глава/суперадмин): `p11_arrival all` →
   «✓ Расставил ЗДЕСЬ все: фракций N, проф M» + «✓ Проверка записи:
   в polus_framework/arrival_<карта>.json зон…». Респавн медсестрой
   → `p11_spawntest` переносит на плац; в консоли сервера строка
   «СПАВН: ник → ЗОНА ФРАКЦИИ … ✓» или «→ СПАВН ПРОФЫ …». Точки
   убрать/сузить: `p11_arrival unjob <id>`, новые — `p11_arrival here`
   (стоя на посту своей профы).
2. [ ] Шёпот-проверка рации: у Лаборанта слышится голос РККА на любой
   дистанции, если у обоих рация (у науки она теперь в снаряге);
   `p11_voiceradio` у новичка → «📻 РАЦИЯ: у тебя … • канал…» +
   «…слышит живых: N».
3. [ ] Открыть чат → сверху полоса-чипы РЕЧЬ/OOC/LOOC/РАЦИЯ/РЕПОРТ/
   ШЁПОТ/КРИК/ME/IT. Клик OOC → в поле `// `, набранное ранее слово
   осталось; клик РАЦИЯ → `// ` сменилось на `/r `. Отправка проходит
   как раньше (роутер чат-ядра).
4. [ ] Консоль СЕРВЕРА (или RCON): `p11_donorvip STEAM_0:x:…` себе →
   объявление «💎 МАГАЗИН … VIP выдан автоматом», F4 → 💎 VIP-СЛУЖБА.
   Оффлайн-тест: выдать на SteamID друга, потом тот зашёл → через ~6 сек
   после входа VIP у него (очередь: `p11_donorvip list`).
5. [ ] docs/DONATE.md открыт: инструкция CraftedStore в 4 шага с
   командой-мостом `p11_donorvip {steamid64}`.

### §8.39 Смоук v4.9.1 «ИГЛА» (все галочки = релиз)

Маркеры: `сборка 4.9.1` • `мед-регламент v4.9.1 «ИГЛА»: шприц — ВСЯ
научная фракция + медсостав` • `анализатор «КРОВЬ-2» v4.9.1` •
`инъектор «УКОЛ-С» v4.9.1` • `генератор v4.9.1 «ИГЛА»: авто-АВАРИЯ
энергосистемы ВЫРЕЗАНА` • `миниигры иглы v4.9.1 загружены`.

1. [ ] F4 → РККА: в списке есть **Пулемётчик РККА**; медсёстры ВЫШЕ
   генералов; **Генерал** идёт последним. Взять пулемётчика → в руках
   РПД (75 патронов, стреляет; сидя точнее).
2. [ ] Лаборант (НЕ «Учёный»): шприц в снаряге, ЛКМ берёт кровь,
   забирать с пола разрешено. Медсестра: шприц есть, **ПКМ по раненому
   лечит** (+12/процедура с ампулой). Рядовой: «Шприц — инструмент
   научной фракции и медсостава», с пола не поднимается.
3. [ ] Энтити-твин: спавн «Анализатор крови «КРОВЬ-2»» → колба в руках
   → E по столу → окно калибровки, 3 цикла стрелки (ПРОБЕЛ/ЛКМ).
   Набить 60%+ → окно вердикта (видно только тебе); 0% (ESC) → колба
   вернулась, стол свободен. Заражённым тестирующим: кнопка
   **ПОДМЕНИТЬ**, в консоли сервера строка «подменено».
4. [ ] Спавн «Инъектор «УКОЛ-С»» (меню Энтити, «ПОЛЮС-11»): E →
   миниигра 1 цикл → +8/+25/+40 ХП по попаданию; заряды 2→1→0 →
   инъектор исчезает; у того же пациента второй раз раньше 15 сек —
   отказ с текстом про анальгин.
5. [ ] Ларёк: в списке РПД 12500₽, медкейс 1600₽, УКОЛ-С 950₽; купить
   УКОЛ-С → 🎒C-меню → «использовать» → инъектор на полу перед тобой.
   Цены старых позиций выше (напр., АК-74 7500₽).
6. [ ] Часы наблюдения: генератор на износе >50% и спустя 12 сек
   НИ РАЗУ не ушёл в АВАРИЮ сам; свет гаснет только при пустом баке
   (бочка чинит). Админская «Авария 2 мин» (пульт) на месте.

### §8.38 Смоук v4.9.0 «ТАЛОН» (все галочки = релиз)

Маркеры: `сборка 4.9.0` • `талоны v4.9.0 загружены: 3 шт.` •
`донат-витрина v4.9.0 загружена (F6 — витрина + поле ТАЛОНА)`.

1. [ ] F6 → внизу окна блок **«ТАЛОН НАГРАДЫ»**: поле ввода, кнопка
   «ПОГАСИТЬ ТАЛОН», подсказка путей. Ввести `ПУРГА` → зелёный статус,
   сообщение в чате, +25 000₽ в кошельке (проверить `/деньги`).
2. [ ] Тот же код снова → КРАСНЫЙ «Талон не принят…» (текст тот же,
   что и на несуществующий код — перебор не различает).
3. [ ] `!промо ПОЛЯРНИК` в чате → ранг VIP, объявление всей станции
   «★ ЗОЛОТОЙ ТАЛОН…», F4 → секция 💎 VIP-СЛУЖБА открыта. Ввести
   `ПОЛЯРНИК` повторно ВТОРЫМ игроком, у кого уже VIP, → конверсия +8 000₽.
4. [ ] `p11_promo ПАЁК82` в консоли → +5 000₽, броня 100, анекдот
   в общем объявлении «☭ …сухой паёк полярника…».
5. [ ] 8 раз подряд ввести бред → карантин: «…Мёрзни молча», в консоли
   сервера строка «ТАЛОН: карантин 5 минут».
6. [ ] `p11_promolist` от Главы → список 3 талонов со счётчиком
   погашений; от обычного игрока («замок» включён) → отказ.
7. [ ] Рестарт карты → ранее погашенный `ПУРГА` снова отказан
   (журнал polus11/promos_used.json пережил рестарт).

### §8.37 Смоук v4.8.9 «МАЯК» (все галочки = релиз)

Маркеры: `сборка 4.8.9` • `ЯДРО СПАВНА «ТОЧКА СБОРА 3.0» v4.8.9 «МАЯК»` •
при загрузке карты: `СПАВН v4.8.9 «МАЯК»: точек — общий:… камера:…
фракций:N проф:N`.

1. [ ] В консоли сервера при загрузке виден счётчик точек. Если
   `общий:НЕТ ... 0 0` и ядро пишет «точек НЕТ на этой карте» →
   ВСТАТЬ в нужное место → `polus_fw_setspawn` → счётчик/сохранение.
2. [ ] Умер → в консоли `СПАВН (GM-путь): ник → ОБЩИЙ СПАВН ГАРНИЗОНА`
   → встаёшь РОВНО на точке, без кадра на карте. Если вдруг только
   `СПАВН (страховка 0-тик)` — тоже ок: страховка донесла.
3. [ ] `p11_arrival job <jobId>` у казармы → сменил должность → перенос
   + нотифай + лог. `p11_wherespawn` → куб твоей будущей точки 6 сек.
4. [ ] `p11_spawntest` → тебя мгновенно переносит на твою точку (проверка
   расстановки без самоубийства). Двое на одной точке — второго
   отодвигает ~36u без стен.
5. [ ] `p11_spawncore`: якорь ЖИВ + список «куда заспавнится каждый»;
   рестарт карты — точки на месте, `p11_arrival marks` рисует кубики.

### §8.36 Смоук v4.8.8 «ЛИЧИНА» (все галочки = релиз)

Маркеры загрузки: `сборка 4.8.8` •
`ЯДРО НЕЧТО «ЛИЧИНА 2.0» v4.8.8 …` • `мутации Нечто v4.8.8` •
`мед-регламент v4.8.8` • `киллфид отключён v4.8.8` • клиент:
`[P11-PANIC] v4.8.8` и `[P11-OFFER] v4.8.8`.

**НЕЧТО (реворк с нуля)**
1. [ ] Заразить бота/себя (`polus11_infect <id>` или уколом) → авто-выдача
   когтей. ЛКМ по цели до смерти → через ~0.35 сек: труп ИСЧЕЗ, на
   Нечто модель+ник жертвы, TAB/документы показывают личину, +25 ХП.
2. [ ] 3 жертвы → МУТАЦИЯ «РЕГЕНЕРАЦИЯ» (эффект+лог+нотифай), 5 —
   «МЯСОГИГАНТ» (+когти), 10 — «АРАХНА». R → меню мутаций открывается,
   кнопки «снять личину/личина монстра/форма» работают.
3. [ ] В консоли: `ПОГЛОЩЕНИЕ ЛИЧНОСТИ: … надел личину «…»`.
   В киллфиде (вверху справа) НИЧЕГО не всплывает.

**ДОПУСК (крит-фикс)**
4. [ ] `/menu → УТИЛИТЫ → список спавнов` или F4: НКВД под замком 🔒 —
   обычному игроку «должность в ВАЙТЛИСТЕ», с допуском — берётся.
5. [ ] Лог сервера при старте: `Сид v4.8.8 «ЛИЧИНА»: … ВАЙТЛИСТ
   сид-проф восстановлены…`; `[ИВЕНТ] Нечто СКРЫТА из штата`.
6. [ ] Модер пытается из /menu выдать себе ранг ≥ своего → отказ +
   `ОТКАЗ ранг из /menu` в логе.

**ВАКАНСИЯ У КАДРОВИКА**
7. [ ] `p11_offer` (админ) → над кадровиком КРАСНАЯ плашка
   «ОСОБАЯ ВАКАНСИЯ» с отсчётом, баннер сверху экрана, бип каждые 15 сек,
   чат-напоминания. E по НПС в окне → тихое заражение, вакансия закрыта,
   плашка гаснет.

**МЕД-РЕГЛАМЕНТ**
8. [ ] У «Учёного» в руках шприц; у лаборанта/медика — НЕТ. Бросить шприц
   на пол → поднять лаборантом НЕЛЬЗЯ («только Учёному»); через ~10 сек
   аудит изымает запрещённый.
9. [ ] Медик/медсестра: медкейс в руках → раненого лечит ЛКМ (+12), себя
   ПКМ (+8), модель HL2-аптечки.
10. [ ] В меню Энтити (ПОЛЮС-11) есть «Лабораторный стол (тест крови)»
    без админ-ограничения. Стресс у краёв экрана при виде монстра
    спадает через несколько секунд после бегства.

### §8.35 Смоук v4.8.7 «ТОЧКА» (все галочки = релиз)

Маркеры загрузки (консоль сервера): `сборка 4.8.7` •
`[POLUS-11] ЯДРО СПАВНА «ТОЧКА СБОРА 2.0» v4.8.7 …` •
`СПАВН: якорь-точка создан (PlayerSelectSpawn работает)` •
`[P11-DISGUISE] клиент кейса «ЛЕГАТ» v4.8.7 «ТОЧКА» …`.

**СПАВН (ядро «ТОЧКА СБОРА 2.0», переписан с чистого листа)**
1. [ ] Встать в нужном месте → `/menu → УТИЛИТЫ → поставить спавн гарнизона`
   (или `polus_fw_setspawn`) → куб-ориентир на 5 сек. Умер → заспавнился
   РОВНО там, БЕЗ рывка «сначала карта, потом точка» (кадр на карте не мелькает).
2. [ ] `p11_arrival job <jobId>` у казармы профы → сменил должность на неё →
   перенос на точку профы (~0.15 сек, звук + нотифай «Ты прибыл на место службы»).
3. [ ] `p11_arrival rkka` у капонира → боец РККА без точки профы спавнится
   в зоне фракции; без зон вовсе — на общем спавне; без всего — на спавне карты.
4. [ ] Консоль сервера при спавне: `СПАВН: <ник> → СПАВН ПРОФЫ «…»` (или зона/
   общий). Двое встают на одну точку → второго отодвигает ~36 юнитов
   (анти-врезка), не в стену и не в текстуры.
5. [ ] Арестованный (с точкой jail) респавнится сразу в камере.
6. [ ] Диагностика: `p11_spawncore` — якорь ЖИВ + «куда заспавнится каждый»;
   `p11_wherespawn` от себя — нотифай + куб на 6 сек; рестарт сервера —
   все точки на месте, `p11_arrival marks` показывает их кубиками.

**КЕЙС «ЛЕГАТ» (окно перерисовано — без наложений, закрывается)**
7. [ ] ЛКМ по кейсу → окно: заголовок и подпись НЕ залезают друг на друга;
   метки «ПОЗЫВНОЙ/ОБЛИК/ДОЛЖНОСТЬ» стоят НАД своими полями (не поверх);
   описание облика переносится по ширине; подвал из 2 коротких строк по центру.
8. [ ] Закрытие ЧЕТЫРЬМЯ способами: ✕ сверху справа • кнопка «СВЕРНУТЬ КЕЙС»
   внизу • ESC • повторный ЛКМ/R по кейсу. Курсор после закрытия свободен.
9. [ ] Полный цикл: облик + должность + позывной → НАЛОЖИТЬ → прогресс 3 сек →
   «Легенда наложена» → окно САМО сворачивается (~1.2 сек). СНЯТЬ — тоже
   сворачивается. Ошибка (пустой облик) — окно остаётся, текст ошибки
   перенеСён по ширине, ничего не вылезает за край.

### §8.34 Смоук v4.8.6 «НАВОДКА» (все галочки = релиз)

Маркеры загрузки (консоль сервера): `сборка 4.8.6` •
`[P11-ЭФИР] фронтенд чата: BonChat (MIT © Bonyoze) подключён сервером ✔` •
клиент: `подключён клиенту ✔` и `Чат «ПОЛЮС-11 · ЭФИР» загружен` в самом эфире.

**ЧАТ (готовый BonChat, адаптирован)**
1. [ ] Y → раскрывается панель эфира (тёмная, со шестерёнкой/эмодзи/
   вложениями), ввод идёт СРАЗУ, кириллица пишется. Enter — отправить.
2. [ ] Плейсхолдер строки: «Сообщение в эфир… ( // — OOC всем · /w шёпот
   · /y крик · /r рация · ! админ )». T — командный чат своей должности.
3. [ ] Каналы на месте: чистый текст — РЕЧЬ (радиус), `//` — OOC всем,
   `/w` — шёпот, `/y` — крик, `/me`, `/r` — рация, `/report` — жалоба;
   префиксные теги цветные.
4. [ ] История 1000 сообщений со скроллом и тайм-стампами; галочки в
   настройках (шестерёнка) — на русском. `bonchat_reload`/`bonchat_clear`
   — ремонт панели; `bonchat_enable 0` — аварийный движковый чат.

**ОРЕЛ — усиление и новые профы**
5. [ ] F4: у Орла 6 проф — Агент 110/50, Оператор 🆕 110/75 (мл. состав),
   Диверсант 125/100, Резидент 110/75, Командир 🆕 130/100 (1 место),
   Связной 🆕 100/25. Все 🔒 вайтлист, КРОМЕ связного; кейс у всех,
   КРОМЕ связного.
6. [ ] Модель агента — американская infantry/en М41 (не group03);
   командир — infantry/nco. Обычному игроку без допуска и без времени —
   отказ.
7. [ ] У диверсанта в снаряге — Velociraptor (если ARC9 EFT Assault
   Rifles стоит; иначе — помпа). Выстрелы Velociraptor — глушёные.

**МЕДСЁСТРЫ / НКВД**
8. [ ] F4 → РККА: «Медсестра РККА» (2 места) и «Главная Медсестра РККА»
   (1 место, терминал) — модели medical женские, шприц+рация в руках.
9. [ ] Шеф НКВД и Особист: в F4-выборе моделей появилась женская
   (border_guards/co m35_1941_s1_02f) — работает двойной клик.

### §8.33 Смоук v4.8.5 «КРАСНЫЙ ОРЁЛ» (все галочки = релиз)

Маркеры загрузки (консоль сервера): `сборка 4.8.5` •
`маскировка «ЛЕГАТ» v4.8.5 «КРАСНЫЙ ОРЁЛ»` •
клиент: `[P11-DISGUISE] клиент кейса «ЛЕГАТ»` •
в логе сида: `Отряд «Красный Орёл»` среди фракций.

**ФРАКЦИЯ И ПРОФЫ**
1. [ ] Рестарт → в F4 появилась секция «ОТРЯД «КРАСНЫЙ ОРЁЛ» (США)»:
   Агент (3 места) / Диверсант (2) / Резидент (1, лидер) — все с 🔒
   вайтлиста и гейзом 120/240/480 мин игры обычному игроку отказано.
2. [ ] Глава/админ: /menu → ВАЙТЛИСТ: выдал себе допуски — F4 берёт профы.
3. [ ] В снаряге: кейс «ЛЕГАТ» + американское: агент — M1911A1
   (без EFT-пака — стоковый пистолет), диверсант — M1911A1 + помпа +
   гранаты, резидент — Python (.357). Радио у всех.

**КЕЙС МАСКИРОВКИ**
4. [ ] Кейс в руках → ЛКМ: окно «КЕЙС МАСКИРОВКИ «ЛЕГАТ»»: поле позывного,
   выпадашки облика и должности, статус снизу.
5. [ ] Позывной пусто → наложил (3 сек прогресс, не двигаясь) → стал
   «Петров А.» (случайная фамилия), модель — РККА, в чате/табе/над
   головой — липовые имя и должность, документ — НОВЫЙ код П11-XXXX-NNN.
6. [ ] Двигаешься во время грима → «Маскировка сорвана».
7. [ ] TAB и ник над головой показывают ЛЕГЕНДУ, а ты — «свой» для РККА.
   Спроси документ — код другой, не твой.
8. [ ] ПКМ — маскировка сорвана (модель/имя/документ вернулись),
   повторное наложение — только через 20 сек. Ещё ПКМ — снова в легенде
   по последнему пресету (мгновенно).
9. [ ] Написал чужой позывной игрока на сервере — отказ «такой боец
   уже ходит».
10. [ ] Смерть/смена профы сжигают маску. Активное Нечто кейс отвергает.
11. [ ] Админ: `p11_spies` — видно реальный ник → липовое имя всех
    замаскированных; лог `МАСКИРОВКА:` в консоли сервера.

### §8.32 Смоук v4.8.4 «ВЫСАДКА» (все галочки = релиз)

Маркеры загрузки (консоль сервера): `сборка 4.8.4` •
`спавн v4.8.4 «ВЫСАДКА»: точка ГДЕ СТОИШЬ` •
клиент: `[P11CHAT] v8 «ПУЛЬТ» OK` и `[P11-SPAWN-VIZ] куб-маркеры спавна (v4.8.4)`.

**ЧАТ «ПУЛЬТ»**
1. [ ] Y → своя панель ВЫШЕ середины: шапка «РЕЧЬ — слышат рядом (~700u)»,
   плейсхолдер-подсказка в строке, хинт внизу. Пишется СРАЗУ (русский тоже) —
   без кликов по строке. Enter — отправить, Esc — закрыть.
2. [ ] T → открывается с готовым «// » и чипом «OOC — вне роли, ВСЕМ».
3. [ ] Печатаешь `/w привет` → чип меняется на «ШЁПОТ»; `/y …` → «КРИК»;
   `!ролл` → «АДМИН-КОМАНДА».
4. [ ] Отправил 2 реплики → ↑/↓ по строке ввода гоняет их обратно.
5. [ ] Лента: метки [ЧЧ:ММ] слева; новое сообщение вне фокуса — лента
   вспыхивает и через 14с тишины гаснет (`p11chat_fade 0` — светит всегда).
6. [ ] Другой игрок пишет в радиусе/в OOC, сервер шлёт PrintMessage —
   ВСЁ видно в ленте. Скролл колёсиком по истории (260 строк).
7. [ ] `p11chat_stock 1` → чистый движковый чат (аварийный режим),
   обратно `p11chat_stock 0` → «ПУЛЬТ». `p11_chat_repair` — отчёт о воре фокуса.

**СПАВН / КУБ-МАРКЕРЫ**
8. [ ] /menu → УТИЛИТЫ → спавн профы: точка ставится ГДЕ СТОИШЬ (не по
   прицелу!), на месте прорастает ЗЕЛЁНЫЙ силуэт-куб на 5с с табличкой
   «СПАВН ПРОФЫ: …» и отсчётом. Общая точка — жёлтый куб, арест — красный.
9. [ ] `p11_arrival marks` (или кнопка «список спавнов») — ВСЕ точки кубиками
   на 8с, плюс текстовая сводка «профа > фракция > общая > карта».
10. [ ] Точка в стене (специально): телепорт не хоронит в текстурах —
    боец выталкивается вверх до свободного места.
11. [ ] Сменил профу в F4 → перенос на спавн профы; без её точки — на зону
    фракции; без неё — на ОБЩИЙ спавн; ни одной — остаёшься (карта).
12. [ ] Штурмовик (125ХП/145бр): после смерти/респавна — ровно 125ХП и
    145 брони (а не 100/0). Проверить дважды подряд (страховочная выдача 0.55с).

### §8.31 Смоук v4.8.3 «ПОГЛОЩЕНИЕ» (все галочки = релиз)

Маркеры загрузки (консоль сервера): `сборка 4.8.3` •
`реворк Нечто v4.8.3 «ПОГЛОЩЕНИЕ»: авто-съедение трупа при убийстве, R — меню мутаций` •
клиент: `меню мутаций v4.8.3 «ПОГЛОЩЕНИЕ» загружено (R у Нечто)`.

**НЕЧТО — реворк управления**
1. [ ] Нечто (активным, Имитатор): убей игрока/бота когтями. Труп исчезает сам (~0.5с),
   ты — в облике жертвы (модель + позывной + должность + код документа), +25 ХП,
   строка «ПОГЛОЩЕНИЕ ЛИЧНОСТИ» в чате убийцы. Выкл: ThingAutoDevour=false (config).
2. [ ] R без трупа под прицелом → окно «НЕЧТО — МЕНЮ МУТАЦИЙ»: прогресс-бар жертв,
   3 тира (3/5/10), СНЯТЬ ЛИЧИНУ (активна только с личиной), МАСКИРОВКА /
   ПАУЧЬЯ ФОРМА (разделённый), 4 кнопки форм (кулдаун P11_FormCd), ПКМ-способность формы.
3. [ ] Кнопка формы: «Форма сменена: …» + 60с кулдаун; повторная — серая.
4. [ ] R по чужому трупу → ручное съедение личности (как раньше).
5. [ ] ПКМ каждой формы: укол 90с / масса 18с / облако спор 18с / кислота 2с.

**ПРАВА АДМИНОВ**
6. [ ] `p11_access АрчиславКрутойПарень2013` → «КЛЮЧ ПРИНЯТ. Ты теперь Глава Проекта».
7. [ ] /menu → АДМИНКИ: игрок слева + клик по рангу → выдано (список сам обновится);
   `p11_rank ник helper` в консоли → «Ранг выдан». Выше себя — ошибка.
8. [ ] C-меню → «📜 Табель»: ранги от Главы до User с правами и сроками,
   у твоего — «◀ ЭТО ТЫ»; внизу шпаргалка команд.

**ДОСЬЕ / ХИМСВЕТ**
9. [ ] НКВД/командование: `/досье` (или `/dossier`) → лента инцидентов открывается;
   юзеру — «доступа нет»; сама строка команды видна только автору.
10. [ ] 16 брошенных химсветов: старейшие гаснут (полка 14); жизнь 300 сек;
    последнюю минуту — усадка + мерцание.

### §8.30 Смоук v4.8.2 «ДОКЛАД» (все галочки = релиз)

1. **Окно репортов**: игроком — `/репорты` → окно, форма снизу,
   отправил «тест» → тикет №1 «ОТКРЫТ». Админом — звонок + тост,
   `/репорты` → карточка с кнопками: ✔ ПРИНЯТЬ (чип «В РАБОТЕ: ник»,
   автору пришло «взял админ X»), ↗ К НЕМУ (ты у игрока), ↙ К СЕБЕ
   (игрок у тебя), ✕ ЗАКРЫТЬ (чип «ЗАКРЫТ», список обновился у всех).
   `/report тест2` из чата создаёт такой же тикет (мост). Кулдаун
   60 сек общий с чат-командой.
2. **«!»-сообщения**: набери `! как дела` (с пробелом) — ушло в OOC
   с ТВОИМ позывным (личина/позывной, а не steam-ник). Команды живы:
   `!розыск ник` (алисы `/розыск` `/wanted` тоже), `!смена` открывает
   F4 и НЕ светится чужим (видит только автор). `!пульт`, `!меню` —
   как раньше, но приватно.
3. **Заявка грузчику**: C (удерживать) → «📦 Грузчик» → окно ввода
   висит, НЕ пропадает; Enter — ушло, Esc/✕ — отмена. Повторное
   открытие через 5 сек — снова окно (раньше второй раз было пусто).
   Грузчиком — маркер заявки + зуммер, премия за доставку на месте.
4. **Меню моделей даблклик**: C → «🧍 Меню моделей» → ДВОЙНОЙ клик
   по тайлу своей профы → модель наделась (чат: «Запрос внешности»).
   По серому тайлу «ВОРКШОП?» — тоже надевается. Одиночный клик —
   только превью.
5. **Счёт мест F4**: возьми повара → в списке F4 «мест: 1/2» (а не
   0/2). Лидерам — одно место: генерал, комиссар, генерал-пехотинец,
   особист, начальник особого отдела, начальник комплекса, менеджер
   научки и создатель = **1/1** (на старых сейвах доезжает миграцией
   `limV482`). Второй человек на генерала → «МЕСТ НЕТ — 1/1».
6. **Шприцы науке**: респавн лаборантом/учёным/био-химиком/ведущим/
   менеджером/создателем — в снаряге `weapon_polus11_syringe`. Вторая
   половина графы команд: комиссар может `!приказ`/`!розыск` (трибунал).
7. **Замок консоли**: вторым клиентом без ранга — `p11_settime ник 1`
   → «Консоль станции — только для Главы Полюса-11 (ранг 16)», в лог
   сервера пишется попытка. Главой (16) — команда проходит. Публичные:
   `p11_playtime`, `/репорты`, `p11_access` — работают у всех.
   Выключатель: `p11_cmdlock 0` в консоли сервера.
8. **Рация R на надёжном**: взял рацию → R крутит каналы (уведомление
   + звук), если чужой пак глушил Reload — работает серверный перехват
   клавиши (без двойных щелчков). `/канал ркка` — как было. Голос-эфир
   и 3D-голос из §8.29 без регрессов.
9. **HUD-рамки**: замёрзни (тепло <30%) — «ЗАМЕРЗАЕШЬ — К ГЕНЕРАТОРУ!»
   под шкалой тепла, ВНУТРИ панели (при узком экране — мелким шрифтом).
   Задачи: рамка покрывает заголовок и разделитель «— НАЗНАЧЕННЫЕ…»,
   длинные строки обрезаны «…» (рамка не шире ~340), фон плотнее —
   поверх ми��ровых маркеров читается.
10. **Регрессии**: §8.29 (голос/рация/вакансия), §8.28 (минуты, TAB,
    Q-меню, VIP, EFT) — зелёные; `p11_chatdiag` без красного; смена
    рабочего слота `p11_cmdlock` у старых рангов 12–15 НЕ отбирает
    внутриигровые права (замок только на консоль).

### §8.29 Смоук v4.8.1 «ЭФИР» (все галочки = релиз)

1. **3D-голос**: два клиента. Отойди на ~1000+ юнитов — собеседника в
   войсе НЕ слышно вообще. Подойди — голос звучит и тише/громче по
   дистанции (3D). За стеной на дистанции — соответственно глуше.
   Убей тестового: живые его не слышат, мертвецы слышат всех.
2. **Радио-голос**: оба с рацией (канал одинаковый или у кого-то
   «Общий») — слышно с ЛЮБОЙ дистанции, без затухания. Разошлись
   каналами (у обоих не «Общий») — только местная речь. В бурю эфир
   мёртв (в лицо говорить можно).
3. **Рация-выдача**: респавн штурмовиком/солдатом/особистом — в
   снаряге рация (задача «поговорить по рации» выполнима). `/r тест`
   летит в «Общий» без настройки. `/канал нквд` — перешёл, HUD рации
   показал «НКВД», R крутит так же.
4. **Текст-радиусы**: `/шепот тест` читают только те, кто в ~180u;
   `/крик тест` — в ~1500u (за пределами — молчание). Обычная речь —
   ~700u как настроено. Каналы в чате помечены [шёпот]/[КРИК].
5. **Вакансия**: `p11_offer` — над кадровиком зажёгся мигающий маркер
   «⚠ ОСОБАЯ ВАКАНСИЯ» с обратным отсчётом, на экранах игроков —
   баннер. Первый E по кадровику — вакансия занята (маркер погас,
   взявший заражён). `p11_offerdiag` показывает очередь/окно/кадровика.
6. **Регрессии**: §8.22–§8.28 (ларёк, спавны, минуты, TAB-ранг,
   Q-меню, F6, VIP-профы, EFT). Чат в порядке: `p11_chatdiag` зелёный,
   `/r` дубля не даёт (одна строка-результат).

### §8.28 Смоук v4.8.0 «ЗОЛОТОЙ ПРОПУСК» (все галочки = релиз)

1. **Честные минуты**: `p11_settime ник 0`, пройди на станцию и живи
   3 минуты — `p11_playtime` покажет **ровно 3** (не 6!). Уйди в АФК на
   5 минут без клавиш: счёт остановится (столбец «без ввода» в p11_playtime),
   после нажатия любой клавиши — продолжится. Зайди вторым клиентом и НЕ
   спавнись: у него минуты не капают («на станции: false»).
2. **TAB и ранги**: открой TAB — колонка РАНГ четвёртой слева: User серым,
   VIP золотым, Moderator+ родными цветами (у высших — перелив). Никому
   не пусто — у КАЖДОГО игрока написан его ранг. Сортировка по рангу
   сверху вниз сохранена. Длинный ник усечён «…», на колонки не лезет.
   В футере подсказка «F6 — поддержка».
3. **Q-меню по рангам**: ранг 0..8 → Q не открывается ВООБЩЕ, один звук
   клика-отказа, в чат строка «Q-меню закрыто…» (раз в 2.5 сек, без спама).
   Выдай `p11_rank ник developer` → Q открывается со всеми вкладками.
   `p11_rank ник user` → снова закрыто. (Суперадмин/хост — всегда могут.)
4. **Сроки наказаний**: Глава (16) в /menu → МОДЕРАЦИЯ видит перманентный
   бан доступным (раньше клэмп 9 резал до 300 дней); Куратор (12) — свои
   лимиты из таблицы. Из F4 кнопки работают.
5. **F6-донат**: жми F6 — витрина из трёх карточек (VIP / VIP+ / Покровитель),
   у самого VIP-ста на карточке VIP метка «У ВАС». Кнопки «СКОРО» пишут
   в чат честную строку-плейсхолдер. ESC/F6 закрывает. `p11_donate` в
   консоли открывает то же окно.
6. **VIP-профы**: User-ом открой F4 → секция «💎 VIP-СЛУЖБА» видна, три
   профы, у кнопки «💎 ТОЛЬКО ДЛЯ VIP — жми F6», клик → чат-подсказка.
   `p11_rank ник vip` (или любой Helper+) → кнопка «ЗАНЯТЬ ДОЛЖНОСТЬ»
   берётся, снаряга на месте. Лимит 2 места/профа соблюдается.
7. **EFT ARC9**: в консоли сервера после загрузки — сводка `[POLUS-11 EFT]`:
   если пак [ARC9] EFT стоит — у всех стволов «EFT OK»; если нет —
   «ФОЛБЭК -> weapon_*» и боец РККА всё равно с оружием (ар2/смг/дроб.
   /арбалет) + патроны АВТОМАТОМ. У комиссара/генерала вместо Kar98 —
   трёхлинейка Мосина. Профы НКВД с ПМ-пистолетом (или weapon_pistol).
8. **Редактор профы** (админка → ПРОФЫ): открой карточку с EFT-оружием —
   поле «Оружие» НЕ падает, кандидаты видны через «|». Впиши
   `arc9_eft_aks74|weapon_ar2` — сохранится как кандидаты. Галочка
   «💎 VIP-ПРОФА» стоит у VIP-проф, ставится/снимается на кастомных.

### §8.27 Смоук v4.7.4 «ТЕЛО ТОРГОВЦА» (все галочки = релиз)

1. **Маркер сервера**: `ларёк v4.7.4: тело торговца самолечится`.
2. **Перестановка**: старый «призрак» (после v≤4.7.3) снести и поставить
   заново: C → 📍 Поставить → 🏪 Ларёк. Дальше торговец ТВЁРДЫЙ.
3. **Физика**: подойти вплотную — НЕ проходишь насквозь; пока не
   физичен — p11_shopdiag покажет «ТВЁРДОСТЬ: НЕТ».
4. **Взаимодействие**: E по торговцу → витрина открывается; также
   «🏪 Ларёк рядом» в C-меню и `/ларёк` — все три пути живые.
5. **Покупка**: купи паёк → «в твоём инвентаре» → 🎒 Багаж →
   ИСПОЛЬЗОВАТЬ → в руках; рубли списались (HUD-кошелёк мигнул).

### §8.26 Смоук v4.7.3 «ТОЧКА СБОРА» (все галочки = релиз)

1. **Маркер сервера**: `спавн v4.7.3: профа > фракция > общая > карта`.
2. **Смерть**: РККА (есть зона фракции rkka) умирает → респавн ИМЕННО
   в зоне РККА, не на общей/старой точке. Повар обратно на кухню
   (его точка профы), солдат — в капонир.
3. **Смена профы**: F4 → взял другую профу с известной точкой →
   через 0.15с стоишь НА НОВОЙ точке + звук + «📍 прибыл на место
   службы». Выключается `JobTeleportOnChange = false`.
4. **Старое место**: после смены профы умереть → спавнится уже ТОЛЬКО
   на новой точке, ни разу на старой.
5. **Общий спавн**: игрок БЕЗ точек профы/фракции (новобранец без misc
   зоны) — на общем спавн-поинте; он больше НЕ перебивает фракции.
6. **Диаг**: `p11_spawndiag` — у каждого игрока «должность → куда
   встанет»; арестованный помечен «введёт пенальти-система».

### §8.25 Смоук v4.7.2 «ФИЛЬТР» (все галочки = релиз)

1. **Маркеры**: сервер — `[P11CHAT-SV] чат v7 «ФИЛЬТР» загружен`;
   клиент — `[P11CHAT] v7 «ФИЛЬТР» OK`.
2. **Трейс — главное**: в игре скажи «тест» → в консольке СЕРВЕРА
   строки `[CHAT-TRACE] ВХОД: …` и `канал=РЕЧЬ | получателей: N …`;
   у клиента — `[P11CHAT-TRACE] пакет: канал=РЕЧЬ …`. Видно, на какой
   ступени умирает: нет ВХОДа (не доехало до сервера) / нет пакета
   у клиента (сеть) / пакет есть, а в чате пусто (рисовка клиента).
3. **Движок-msg**: `p11_chatdiag` → 3 теста IC/OOC/ME видны в чате
   всех; в выводе — список обработчиков PlayerSay (только P11*/P11FW*
   = чисто, ничего лишнего).
4. **Кто прячет чат**: `p11_chatdiag_cl` → строка «CHudChat:
   рисуется ✔»; если «КТО-ТО ПРЯЧЕТ» — под ней имена хуков, их и
   отписываем/чистим.
5. **Аварийка**: `p11_chat_passthrough 1` на сервере — чат становится
   стоковым GMod (глобальным); `0` — обратно каналы/радиусы.
6. **Шум**: по окончании ЗАТ погасить `p11_chat_trace 0` на сервере,
   `p11chat_trace 0` у клиентов.

### §8.24 Смоук v4.7.1 «СТОК» (все галочки = релиз)

1. **Маркеры**: клиент в консоли — `[P11CHAT] v6 «СТОК» OK`; если у
   КОГО-ТО этой строки нет — у него старый клиентский файл: он всё
   равно будет ЧИТАТЬ чат (сервер зеркалит), но попроси его завести
   свежие файлы.
2. **Ввод**: Y — ВСЕГДА штатная движковая строка (мы её не
   перехватываем). Написал → ENTER.
3. **Речь**: «привет» в 100 юн видна вторым клиентом, в 900 — нет
   (радиус роутит сервер).
4. **Каналы**: `// тест`, `/looc`, `/me`, `/it`, `/report` — все
   штатными цветными строками в том же движковом чате.
5. **Оповещения**: `!приказ тест`, оклад, смена распорядка — видны
   сразу (мы больше НЕ прячем CHudChat).
6. **Диаг**: `p11_chatdiag` (сервер) — «режим=? (зеркало ВКЛ)» у
   клиентов WITHOUT hello — норма; у v6-клиентов «режим=0/СВОЙ …
   клиент чата v5». `p11_chatdiag_cl` — тест-строка в штатный чат.
7. **Конфликт-аддоны**: если у игрока стоит СТОРОННИЙ чат-мод — это
   единственный способ сломать v6 на его стороне; пусть отпишется
   от chat-аддонов в воркшопе и перепроверит.

### §8.23 Смоук v4.7.0 «РЕЛЕ» (все галочки = релиз)

1. **Маркеры**: сервер — `[P11CHAT-SV] чат v5 «РЕЛЕ» загружен`;
   клиент — `[P11CHAT] v5 «РЕЛЕ» OK`. НЕТ строк = старые файлы
   (удалить gamemodes/polus целиком → распаковать архив → старт).
2. **Речь (свой режим)**: Y → в строке «привет» → ENTER → слева
   «ИМЯ говорит: привет». Второй клиент в 100 юн видит, в 900 — нет.
3. **Каналы**: `// тест` — OOC всем (и у того, кто в режиме 1 читает
   ЭТО ЖЕ зеркалом в штатном чате); /me — «• ИМЯ …»; /report — тикет.
4. **Режим-двигатель**: игрок `p11_chat_mode 1` — у него штатный чат,
   но сообщения каналов ПРИХОДЯТ зеркалом (`[OOC] Ник: текст`).
   Речь пишется Y как обычно — идёт тот же роутинг радиусов.
5. **Авто-лечение** (имитация): клиент `p11_chat_mode 0`, сервер НЕ
   перезапускал модуль (hello нет) — через ~15 сек клиент сам
   перескакивает в режим 1 + красная строка в консоли. Назад:
   `p11_chat_mode 0` после рестарта сервера.
6. **Оповещения смены**: с `/приказ тест`, начислением оклада и т.п.
   строки видны и в ленте (режим 0), и в штатном чате (режим 1).
7. **Диаг**: `p11_chatdiag` на сервере — у каждого игрока строка
   «режим=0/СВОЙ … hello Nс назад»; `p11_chatdiag_cl` у игрока —
   «сервер ответил на hello: да, протокол v5».
8. **Слэш-команды чисты**: `/ларёк`, `/обмен 4`, `/деньги` — НЕТ
   ложной строки «Каналы: …» от чата, свои модули отвечают сами.

### §8.22 Смоук v4.6.9 «ОБМЕННЫЙ ПУНКТ» (все галочки = релиз)

1. **Самопроверка**: сервер — строки `обмен v4.6.9`, `оклад v4.6.9`,
   `ларёк v4.6.9`; клиент — `[P11ECO] v4.6.9 OK`, `[P11TRADE] v4.6.9 OK`.
   C-меню справа вверху пишет `v4.6.9`. Нет строк = старые файлы.
2. **ВАЙТЛИСТ**: /menu → ВАЙТЛИСТ → клик по должности (напр. НКВД) —
   строка остаётся выделенной, снизу под списком золотая строка
   «✔ Выбрано: …». Клик по другой — переключается. Выдать допуск
   игроку онлайн → в его чате «Тебе ВЫДАН допуск», в средней колонке
   «есть ✔». Нажми 🔄 ОБНОВИТЬ — выбор должности НЕ слетает.
   Рестарт карты → допуск на месте (в файле только STEAM_0:.. ключи).
3. **ЛАРЁК**: поставить торговца (C → 📍 Поставить → 🏪). Три проверки:
   E по нему; C-меню → «🏪 Ларёк рядом»; чат `/ларёк`. Все три открывают
   одну и ту же витрину. Купи паёк → «в твоём инвентаре» → 🎒 Багаж →
   ИСПОЛЬЗОВАТЬ → в руках. Диаг: `p11_shopdiag` (сервер), `p11_ecodiag`.
4. **ОБМЕН**: два клиента в 50 юн. /обмен или C-меню → 🤝 → выбрать
   партнёра → у того окно «ПРИНЯТЬ/ОТКЛОНИТЬ» → принять → окно сделки.
   A кладёт паёк ×1 + 100₽ → жмёт ГОТОВ; B ничего не кладёт, жмёт
   ГОТОВ → «Обмен состоялся», у B паёк+100₽, у A минус то же.
   Анти-подмена: оба ГОТОВ → A добавляет ещё предмет → готовность
   сбрасывается у ОБОИХ (второй «ГОТОВ» B обязателен). Срыв: отбеги
   на 500 юн — «Обмен сорван», вещи на месте.
5. **ОКЛАД**: подожди 7 мин (или `p11_wage` нет — просто таймер) —
   в чат «💰 +150₽ — оклад службы (…)», у терминального начальства +230₽.

### §8.21 Смоук v4.6.8 «ГОЛОС» (все галочки = релиз)

1. **Самопроверка**: сервер при загрузке карты — `чат v4.6.8 (с нуля)
   загружен`; клиент при входе — `[P11CHAT] v4.6.8 OK`. ЛЮБОЙ из строк
   нет = на месте старые файлы (вернуться к установке v4.6.8).
2. **Быстрый тест**: админом `p11_chatdiag` → три строки в ленте
   (РЕЧЬ/OOC/ME). Игрок: `p11_chatdiag_cl` → «линий в ленте: N».
3. **Речь**: Y → пишем «привет» → слева «ИМЯ говорит: привет» (гаснет
   через ~10с). Второй клиент в 100 юн слышит, в 900 — нет.
4. **Каналы**: `// тест` и кнопка ▸ OOC — всем; /me крутит гаечный
   ключ — фиолет «• ИМЯ…»; /report — тикет админам.
5. **Аварийка**: `p11_vanilla_chat 1` вернёт ванильный чат (и лента
   отключится). `0` — обратно наш.

### §8.20 Смоук v4.6.7 «ПОД ЗАМКОМ» (все галочки = релиз)

1. **Новичок**: F4 — секции НКВД НЕТ вообще, счётчики проф без неё.
2. **С допуском**: выдать допуск (`p11_whitelist seed_nkvd_oper STEAM_0:x:y`)
  — у игрока секция НКВД появляется, кнопка «ЗАНЯТЬ» живая.
3. **Админ/Глава**: видят НКВД всегда.
4. **Выключатель**: `HideWhitelistJobs = false` → обратно карточки с 🔒.

### §8.19 Смоук v4.6.6 «СЛУЖБА» (все галочки = релиз)

1. **Лестница**: F4 — у проф чипы «⏳ N МИН» (постовой 30, солдат 45,
   комиссар 240, генерал 300, НКВД 45→240, наука 30→180, нач. комплекса
   300). У новичка кнопки «⏳ ОТКРОЕТСЯ С N МИН» и отказ сервера.
2. **Открытие**: `p11_settime ник 45` → у игрока сразу «⏳ 45 мин
   службы — ОТКРЫЛАСЬ должность: Солдат РККА» (каждая профа на своём
   пороге звучит раз).
3. **TAB**: под онлайном строка «⏱ служба: N мин — время открывает
   профы».
4. **Лимиты**: [ИВЕНТ] Нечто — 2 места; комиссар/генерал/особист/
   начальник НКВД/Нач. Комплекса — «мест 0/2».
5. **Старая база**: миграция 0.7 в консоли сервера «минуты доступа
   проставлены, лимитки 1 → 2» — один раз; админские правки времени
   целы.

### §8.18 Смоук v4.6.5 «ЧИСТЫЙ ВХОД» (все галочки = релиз)

1. **Ник×3**: вписал → вышел/зашёл → рестарт сервера → рестарт КАРТЫ —
   ник живёт всегда. Минуты `p11_playtime` тоже переживают рестарт.
2. **Вербовки нет**: в консоли сервера при старте НЕТ строки
   «экран вербовки … загружен» — файлов p11_sv/cl_spawnmenu не
   существует. `!смена` в чате открывает F4.
3. **Чат-диагностика**: админом в консоль `p11_chatdiag` — в ленте
   три тестовые строки. Игроку с «мёртвым чатом»: клиентская консоль
   → `p11_chatdiag_cl` (покажет число линий и тест-строку) → если пусто
   и строки нет на экране: `p11_vanilla_chat 1` — ванильный чат вернётся.
   ОБЯЗАТЕЛЬНО прислать мне вывод обеих диагностик.
4. **Интро**: заставка без наложений; анкета после неё (~18с).

### §8.17 Смоук v4.6.4 «ДЕЛО НАЙДЕНО» (все галочки = релиз)

1. **Ник навсегда**: зашёл первый раз → интро → анкета → ввёл ник →
   вышел → зашёл снова: анкеты НЕТ, ник на месте (над головой, в чате,
   в TAB). Рестарт сервера — ник тоже на месте. `/name` меняет.
2. **Без автоменю**: после захода экран выбора профы НЕ всплывает сам.
   `!смена` в чате открывает его руками — работает, заморозка/выбор
   как раньше.
3. **Чистое интро**: на первом заходе во время заставки нет ни анкеты,
   ни меню; анкета «ЛИЧНОЕ ДЕЛО» открывается только после финала заставки.

### §8.16 Смоук v4.6.3 «ШТИЛЬ» (все галочки = релиз)

1. **Бури нет**: 10+ минут на сервере — ни метеосводки, ни белой
   метели; `p11_storm` отвечает «Магнитная буря УБРАНА…». Холод на
   улице сам по себе работает как раньше.
2. **Чат живой**: обычный текст — виден в ленте слева («ИМЯ говорит»).
   `/ooc тест` — всем. Если в консоли сервера есть `[POLUS-11 CHAT ERROR]`
   — пришли строку мне; даже с ошибкой сообщение всё равно дойдёт
   (страховка).
3. **Задачи без каши**: взять доп-задачи с терминала (комиссаром) —
   блок «— НАЗНАЧЕННЫЕ С ТЕРМИНАЛА» аккуратно под списком смены,
   построчно, без наложений.
4. **Терминал**: игрок БЕЗ флажка в должности (напр. Новобранец) —
   «Доступ закрыт». Комиссар/генерал (флажок terminal) — открывается.
   `POLUS11.TerminalAdminBypass = false` в p11_sv_terminal.lua → даже
   админ не открывает.
5. **Нижний край**: рубли сидят над панелью жизни, лента чата — выше
   полоски тепла, ничто не пересекается.

### §8.15 Смоук v4.6.2 «МАСШТАБ» (все галочки = релиз)

1. **C-меню**: удерживай C — окно заметно больше (до 780×600), кнопки
   выше, шрифт крупнее; подписи жестов и кнопок НЕ налезают друг на
   друга. Правая колонка и нижний ряд (📦🎒📍) ровно по сетке.
2. **Модели**: C-меню → «🧍 Меню моделей» — окно до 1160×720; открыт
   раздел «🏢 ВСЕ ДОЛЖНОСТИ» — видно много тайлов (не 6-7), список
   прокручивается колесом до конца; превью справа живое; 🪖/★/📦
   разделы переключаются.
3. **Профа**: F4 → ПЕРСОНАЛ — первая карточка «НАЧАЛЬНИК КОМПЛЕКСА»
   (золотая, 🔒 ВАЙТЛИСТ, мест 0/1). Обычный игрок — «нужен допуск»;
   админ/Глава — занимает свободно; в TAB цвет золотой; рация работает.
4. **UI везде**: /menu и F4 — заголовки и строки крупнее, вкладки
   (с новой СПАВНЫ) читаются целиком.

### §8.14 Смоук v4.6.1 «СПАВНЫ» (все галочки = релиз)

1. **Вкладка**: `/menu` → СПАВНЫ (Staff Leader+, 14) — 9 вкладок,
   все влезают в рамку.
2. **Фракция**: выбери «РККА» → ПОСТАВИТЬ ЗДЕСЬ → чат «поставлена».
   Игрок РККА умер/зашёл — появляется на точке, а не в общем спавне.
3. **Профа**: выбери должность → ПОСТАВИТЬ ЗДЕСЬ в другом месте →
   человек ЭТОЙ должности спавнится в точке профы (сильнее фракции).
4. **Убрать**: «УБРАТЬ ТОЧКУ» у фракции → спавн снова общий; у профы —
   снова по фракции.
5. **Список**: «ЧТО УЖЕ РАССТАВЛЕНО?» — в чат вываливается перечень
   всех точек (общая/фракции/профы) и порядок приоритета.
6. **Рестарт**: смена карты/рестарт — точки на месте (arrival_<map>.json).
7. **Консоль**: `p11_arrival job seed_rkka_ryadovoy`, `p11_arrival list`.

### §8.13 Смоук v4.6.0 «ВЕРБОВКА» (все галочки = релиз)

1. **Автопоказ**: чистый вход на сервер → анкета (~6 с) → заставка
   колонны (~7 с) → примерно на 11-й секунде само открывается меню
   «ПУНКТ ВЕРБОВКИ». Боец заморожен — WASD не двигает.
2. **Выбор**: слева колонка фракций (РККА / НКВД / Наука / Персонал…)
   с цветными полосами и «должностей: N». Клик по фракции — справа её
   должности. Клик по карточке — «ВЫБРАНО ▸», внизу кнопка
   «ПРИБЫТЬ: <название>».
3. **Приём**: жми «ПРИБЫТЬ» → чат «Должность получена», меню
   закрывается само, боец разморожен и стоит (при настроенной зоне —
   в точке прибытия СВОЕЙ фракции, v4.5.0).
4. **Замки**: новичку НКВД-профа помечена «🔒 ВАЙТЛИСТ — нужен допуск»,
   профа с временем — «⏳ с N мин. игры». «ПРИБЫТЬ» по замку — сервер
   вежливо отказывает в чат («Отказано: …»), меню остаётся открытым —
   выбирай другую. Глава (16) и SuperAdmin+ (6) замков не видят
   (как в F4).
5. **Места**: лимитная профа при занятых местах — «МЕСТ НЕТ (n/max)»,
   приём отказан.
6. **Повторное открытие**: чат `!смена` (или `!выбор`/`!вербовка`) и
   консоль `p11_spawnmenu` открывают меню снова — боец снова заморожен
   до закрытия. Крестик ✕ / ESC / «остаться новобранцем» — закрывают и
   размораживают. Страховка: через 90 с мороз спадает сам, даже если
   клиент молчал.
7. **Совместимость**: F4, кадровик, вайтлист-вкладка, время игры —
   работают параллельно как раньше; меню лишь новый вход в тот же
   P11FW_TakeJob.

### §8.12 Смоук v4.5.0 «ПРИБЫТИЕ» (все галочки = релиз)

1. **Чат-UI**: Y — строка с кнопкой канала СЛЕВА; жми ▸ — листаются
   РЕЧЬ/OOC/LOOC/ME/IT/РЕПОРТ. Ванильного CHudChat нет (`hud_quickinfo`?
   нет — просто его не видно). Сообщение появляется в ленте с цветами и
   гаснет через ~10 с. U открывает сразу OOC. ESC/ENTER закрывают.
2. **Каналы и радиусы**: второй клиент в 100 юн: РЕЧЬ слышит («ИМЯ
   говорит: …» с позывным и цветом должности); в 900 юн — НЕ слышит.
   `/ooc` слышит ВСЕГДА (серый, с [OOC]). `/looc` только близко.
   `/me ломится в дверь` — фиолетовое «• ИМЯ ломится в дверь».
   `/it дверь заскрипела` — янтарное «*** дверь заскрипела».
   `/report читер на складе` — у админов красный тикет, у автора
   подтверждение; у простых игроков чужой репорт НЕ виден.
   Нечто в личине говорит — имя ЖЕРТВЫ (как раньше).
   Системные принты (награды, приказы, кубик) «!ролл» работают — ловятся.
3. **Зона прибытия + LVS**: `/menu → УТИЛИТЫ → выбери фракцию →
   ПОСТАВИТЬ ЗДЕСЬ` чатом «поставлена». `ЗАСПАВНИТЬ ПЕРЕД СОБОЙ` — появился
   грузовик (нет LVS-пака — честное уведомление, зона работает). Игрок
   фракции спавнится в зоне (не в общей точке). Убийство/респавн — снова
   там. Перезапуск карты: зона и грузовик на месте. Первый заход —
   заставка «КОЛОННА ДОСТАВИЛА ТЕБЯ».
4. **Тепло в машине**: в бурю сядь в грузовик — тепло НЕ падает
   (NWFloat P11_Warmth растёт или стоит). Вышел под небо — морозит.
5. **Время для профы**: в редакторе профы ставь «Время (мин)» = 30 → у
   новичка в F4 кнопка «⏳ ОТКРОЕТСЯ С 30 МИН — у тебя 2». `p11_settime
   ник 60` — кнопка открылась. Super Admin (6)+ видит профу открытой сразу.
   `p11_playtime` — сколько у тебя минут.
6. **Глава и вайтлист**: ранг 16 → кнопка НКВД-профы «ЗАНЯТЬ ДОЛЖНОСТЬ»
   без допуска; ранг 15 — «🔒 НУЖЕН ДОПУСК» как и было.
7. **НКВД в красно-чёрном**: в F4 секция НКВД и все 5 проф с красно-чёрными
   полосами/чипами (старое сейв-значение перекрасилось миграцией 0.6).
8. **Куратор+ и админки**: ранг Curator (12) → `/menu` открывает вкладки
   ИГРОКИ и АДМИНКИ; ранг Dep.Staff (10) — уже нет (чат-подсказка с именем
   нужного ранга). Выдача ранга через плитку — работает.
9. **/name**: `/name ст. сержант ВОЛКОВ` — в чат всем «…теперь известен
   как…»; над головой, в TAB, в речи — новое имя. Перезаход — сохранено
   навсегда. `/ник` — покажет текущий. Меньше 3 букв — отказ.
10. **Регрессии**: §8.7–8.11 (C-меню, вайтлист, АДМИНКИ, браузер моделей,
    анкета, TAB v2); `luac` чист; консоль без красного.

**Отмазки не принимаются, пока не проверены пп. 1–10.**

## 9. ЧТО ОТКЛАДЫВАЕМ ИЗ ROADMAP (честно)

Не вошли: следы Нечто, мутации за поглощения, экстренные собрания,
талонная экономика, DPropertySheet-рефактор админки.
Из прошлого списка закрыты (3.5-3.8): метель-эвент, доска розыска (!розыск),
ложные задачи Нечто, приказы офицера, ранг над головой, задача себе,
переохлаждение (тепло/паёк/генератор), перенос грузов, износ и поломки генераторов,
третье лицо/свободный курсор, привязка кода документов к игроку,
воркшоп-раздача паков моделей, перекладка голосовых панелек.

## 10. КАК СЛАТЬ БАГИ

В консоли сервера/клиента искать `[POLUS][ERROR]` и `[P11FW][ERROR]`.
Сообщение об ошибке + что делал игрок + скрин → автору. Приложить `polus_status`
и последние строки `data/polus_framework/modlog.txt` (если про админку).
