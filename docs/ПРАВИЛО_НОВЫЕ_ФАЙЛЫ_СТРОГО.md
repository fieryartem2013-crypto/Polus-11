# 🚫 ЖЁСТКОЕ ПРАВИЛО: ВСЕГДА НОВЫЙ ФАЙЛ

> Владелец (11.08.2026): «всегда делай новый файл, не обновляй старый, делай новый ФАЙЛ, вбей в себя».

## ЗАПРЕЩЕНО (никогда)
- ❌ Редактировать ЛЮБОЙ существующий файл сборки.
- ❌ `init.lua` / `cl_init.lua` — их НЕЛЬЗЯ трогать даже для «одной строки» (список загрузки).
- ❌ Перезаписывать/дописывать `p11_*.lua`, `fw_*.lua`, `shared.lua` и т.д.

## РАЗРЕШЕНО (только так)
- ✅ Каждое изменение = НОВЫЙ файл.
- ✅ Новые модули и фиксы кладутся в **`garrysmod/lua/autorun/`**:
  - `lua/autorun/server/*.lua` — серверная логика (грузится сама, после гейммода);
  - `lua/autorun/client/*.lua` — клиентская логика (грузится сама на клиентах);
  - `lua/autorun/shared/*.lua` — общее.
- ✅ Энтити — новые папки в `gamemodes/polus/entities/entities/<name>/` (GMod регистрирует сам).
- ✅ Старые файлы остаются на диске навсегда (сувениры), их не удаляют.

## ПОЧЕМУ ТАК
- Старые файлы целы → сборку нельзя «убить насмерть».
- Откат = удалить один новый файл из autorun.
- Никакой «починки по памяти»: всё новое отдельно, всё старое нетронуто.

## ПРИМЕР
Нужно добавить фикс → создаю `garrysmod/lua/autorun/server/p11_sv_fix.lua`
(и/или `.../client/...`), в нём весь код. Ничего не правлю.

## СТАТУС v5.2.5
- `init.lua` / `cl_init.lua` — В ОРИГИНАЛЕ (возвращены).
- Все фичи v5.2.5 (медали вырезаны, дежурства, фиксы админки и F6, чат-команда) —
  в НОВЫХ файлах `lua/autorun/server/p11_sv_v525_autorun.lua`
  и `lua/autorun/client/p11_cl_v525_autorun.lua`.
