-- ============================================================
--  ПОЛЮС-11 — ЗАПРЕТ СПРЕЕВ (server) v4.14.3 «ЗАРЯД»
--  Заявка владельца: «убери возможность ставить спрей».
--  PlayerSpray: вернуть true = декаль НЕ ляжет. Глухо и молча:
--  ни лагов от чужих картинок, ни NSFW на стенах гарнизона.
-- ============================================================

hook.Add("PlayerSpray", "P11.NoSpray", function(ply)
    return true -- спрей не ставится
end)

print("[POLUS-11] ЗАПРЕТ СПРЕЕВ v4.14.3 «ЗАРЯД»: PlayerSpray глухо выключен — декали бойцов больше не липнут к станции")
